# 衔光管家（Clawbot）项目手册

| 项 | 内容 |
|---|---|
| 文档版本 | v1.3 |
| 更新日期 | 2026-09-21 |
| 手册性质 | 面向团队与维护人员的**工程化手册**，以当前工作区代码为唯一事实依据 |
| 适用范围 | 衔光管家（Spring Boot 3 + Vue 3 全栈实现，`clawbot`） |
| 文档状态 | 迭代 1 / 2 / 3 已全部交付并验证（真实冷启动通过）；迭代 4 已交付 **W4 B-5 合规报告（含前端导出入口）**、**W1 四层异常埋点补全**、**W6 个人状态库地基**、**W6-b 状态库后台**、**W2 灰度开关与熔断回滚**、**W3 工具插件化扩展接口**（当前基线 **485 测试全绿**，Flyway 至 V1.0.14）；其余标注「规划」的迭代 4 能力尚未实现，不得视为已完成 |
| 权威基线 | `微信Claw助手需求规格说明书.md`（SRS-CLAWBOT-002）为唯一权威需求文档 |

> **阅读指引**：**[1. 项目概述](#第-1-章-项目概述)** → **[2. 核心功能说明](#第-2-章-核心功能说明)** → **[3. 技术架构](#第-3-章-技术架构)** → **[4. 开发规范](#第-4-章-开发规范)** → **[5. 部署流程](#第-5-章-部署流程)** → **[6. 维护指南](#第-6-章-维护指南)** → **[7. 迭代交付、证据与规划](#第-7-章-迭代交付证据与规划)** → **[8. 附录](#第-8-章-附录)**。若只关心下一步做什么，直接看 **[7.7 迭代 4 规划](#77-迭代-4-规划前瞻)**。
>
> **诚实性声明**：本手册中标注「已实现」的能力均有对应 `main` 源码与测试为证；标注「规划」的能力属于重新定位后的下一阶段目标，当前未实现，本手册不作完成度暗示。凡本机未取得实证者一律标注「未取得证据」，不计入完成。
>
> **本文档为项目唯一工程手册**：原有 MVP / 迭代 2 / 迭代 3 的 PRD、架构、QA、验收、图与证据索引等过程文档已并入本手册并下线（见 [8.3 文档归档说明](#83-文档归档说明)）；权威需求仍以 SRS-CLAWBOT-002 为准。

---

## 目录

- [第 1 章 项目概述](#第-1-章-项目概述)
- [第 2 章 核心功能说明](#第-2-章-核心功能说明)
- [第 3 章 技术架构](#第-3-章-技术架构)
- [第 4 章 开发规范](#第-4-章-开发规范)
- [第 5 章 部署流程](#第-5-章-部署流程)
- [第 6 章 维护指南](#第-6-章-维护指南)
- [第 7 章 迭代交付、证据与规划](#第-7-章-迭代交付证据与规划)
- [第 8 章 附录](#第-8-章-附录)
- [第 9 章 接手必读（Handover）](#第-9-章-接手必读handover)

---

## 第 1 章 项目概述

### 1.1 项目定位

衔光管家（Clawbot）定位为：**微信原生的单拥有方「个人 / 家庭执行型智能管家」**——一个装好即用、深读你的微信世界、能主动把事办了的专属助手。

核心特征：

- **微信原生入口**：以微信公众号 / 企业微信为唯一交互面，直接消费微信上下文（消息、群聊、文件、收藏、通讯录标签）。
- **执行型而非纯对话型**：核心目标是"把事办了"——记忆用户偏好、跨会话管理任务、主动提醒与跟进。
- **单拥有方、自治部署**：面向个人 / 家庭 / 小社群主理人，实例私有、数据私有，不依赖任何中心化平台。
- **主动触达**：通过微信模板消息 / 订阅通知 / 群@ 等能力，在无需用户发消息时也能行动。

> 与"通用对话助手"的本质区别：通用对话助手是"你问它答"；衔光管家是"你不用问，它替你记、替你盯、替你办"的私人执行体。现实对标范式是个人 Agent 的自主执行，而非聊天机器人。

### 1.2 目标用户与价值

| 用户 | 典型场景 | 衔光提供的价值 |
|---|---|---|
| 个人用户 | "把刚才群里说的聚餐时间记下来，到点提醒我" | 读群上下文 + 主动推送提醒 |
| 家庭 / 宠物主 | "记下猫咪疫苗下一针日期，提前一周提醒" | 长期记忆 + 微信订阅通知 |
| 小社群主理人 | "每周一把本周安排汇总发到群里" | 群@ + 定时触达 + 微信内动作 |
| 自由职业者 | "把文件传输助手的发票整理成记账" | 读取微信文件 / 收藏 + 结构化档案 |
| 隐私敏感用户 | "我的对话和档案只能在我自己的服务器" | 单拥有方自治部署，数据不出私域 |

价值主张：**把微信从"聊天工具"升级为"私人执行中枢"**——对话只是控制面，行动才是主产品。

### 1.3 核心能力清单

| 能力 | 状态 | 说明 |
|---|---|---|
| 微信通道接入 | 已实现 | 公众号 / 企业微信回调、消息收发、媒体与事件 |
| 对话与模型中枢 | 已实现 | LLM 驱动的多轮对话、Agent Loop、SSE 观测（真 token 流式见 2.12 局限） |
| AI 工具（Function Calling） | 已实现 | 6 个内置工具（视觉 / 语音 / 快递 / 路线 / 宠物档案 / 天气示例插件） |
| 反幻觉与一致性校验 | 已实现 | 兜底文案、声明不支持断言、执行一致性校验 |
| 审计与安全基线 | 已实现 | 全量操作审计、越权拦截、敏感脱敏 |
| 限流与成本预算 | 已实现 | 用户 / IP 令牌桶、白名单豁免、Fail-Open |
| 数据保留与归档 | 已实现 | 180 天清理、细粒度删除、匿名化 |
| 在线配置热生效 | 已实现 | 后台改配置免重启生效、变更留痕 |
| 回放调试台 | 已实现 | 工具调用干跑重放、只读护栏 |
| 多模型路由 | 已实现 | 运行时按 provider 路由、真实生效者可查 |
| 管家设置后台 | 已实现（转型） | 原管理后台轻量化，转为"管家设置" |
| 环境自检（Startup Doctor） | 已实现 | 启动/手动触发逐项连通性、运行模式与配额体检（A-1，`GET /api/doctor`） |
| 流式输出观测 | 已实现 | SSE 观测通道（`/api/sse/console`）；事件总线 + 断线续传（FR-08） |
| 会话与调用监控 | 已实现 | 概览/趋势/成功率/意图/延迟/降级六类只读聚合（FR-17） |
| 降级与拦截看板 | 已实现 | 降级率、四层异常分布（L1/L2 埋点已补全，W1）、幻觉拦截计数、"对外泄漏 = 0" KPI（A-3） |
| 档案变更留痕 | 已实现 | 写操作字段级 before/after diff，可查询变更历史（A-4） |
| 超时预算瀑布图 | 已实现 | SC-01~SC-05 单次链路时序瀑布，预算线可核查（A-5） |
| 任务型多步会话 | 已实现 | 槽位填充续接、追问 ≤3 次放弃、10min 超时清理（FR-24） |
| 合规报告自动化 | 已实现（W4/B-5） | 一键导出留存/删除执行合规报告（Markdown，SUPER_ADMIN 独占，BR-21 脱敏）；后台已有导出入口页 `/compliance/export` |
| 个人状态库 | 已实现（W6 地基 + W6-b 后台） | 跨会话生长的个人知识（人/地/物/偏好/惯例）+ 溯源 + 召回注入 + 后台只读列表与纠错删除；承诺类条目待 W8 |
| 灰度开关与熔断回滚 | 已实现（W2/FR-22） | 白名单 + 稳定分桶比例放量（默认 100 = 零行为变更）、30s 熔断自动回滚、一键回滚端点与预览端点（BR-31 回滚路径）；灰度观测台前端页待 W2-b |
| 工具插件化扩展 | 已实现（W3/FR-23） | 工具自述派生口径 + 注册表反查，新增工具零改对话引擎；非法 Schema/重名阻止启动（Fail-Fast）；工具分组授权与工具级限流未实现 |
| 事件与触发引擎 | 规划（迭代 4） | 微信事件 / 定时 / 状态变化驱动主动行动 |
| 主动执行与承诺闭环 | 规划（迭代 4） | 提醒、跟进闭环、微信触达 |

### 1.4 技术栈一览

| 层 | 技术 |
|---|---|
| 后端 | Spring Boot 3.x（JDK 17+）、MyBatis-Plus、Flyway、Spring Security/JWT、Resilience4j |
| 前端 | Vue 3 + TypeScript + Vite + Element Plus + Pinia + axios + ECharts |
| 认知中枢 | 大语言模型（LLM，`LlmClient` 抽象，支持多 provider 路由） |
| 存储 | MySQL 8.4（开发库 3306）、Redis（缓存 / 限流 / 分布式锁 / 去重 / JWT 黑名单） |
| 通道 | 微信公众号 / 企业微信（XML 回调 + 消息 API） |
| 架构形态 | 分层单体（Modular Monolith），非微服务 |
| 部署 | 单拥有方自治部署（Docker Compose / 裸机 jar） |

> 技术栈每项须有真实引用点；标注"预留未启用"的项不得视为已生效。

### 1.5 代码库目录结构

```
clawbot/
├── backend/                 # Spring Boot 后端（包根 com.lumensteward.clawbot）
├── frontend/                # Vue 3 前端（管家设置后台）
├── docs/                    # 本手册
├── scripts/                 # e2e-smoke.ps1 等
├── docker-compose.yml       # 单拥有方部署编排
├── README.md                # 平台入口
└── 微信Claw助手需求规格说明书.md  # 权威需求文档（SRS-CLAWBOT-002）
```

### 1.6 重新定位说明（演进记录）

本项目早期基线（迭代 1~3）实现的是一个"微信对话助手内核"（LLM 中枢 + 工具 + 审计 + 限流 + 归档 + 配置 + 回放 + 多模型）。为使产品定位清晰、避免与"通用对话助手"同质化，项目完成一次**定位重锚**：

- **从**：通用对话助手（你问它答）。
- **到**：微信原生的私人执行型管家（你不用问，它替你记、盯、办）。

重锚后的目标架构在既有内核之上增加**三层执行体能力**（个人状态库 / 事件与触发引擎 / 主动执行器，均属规划·迭代 4），使系统从 turn-driven 转为 event-driven。既有内核（迭代 1~3）全部可复用，无需重写。

---

## 第 2 章 核心功能说明

> 章节前缀「已实现」表示有对应 `main` 源码与测试；「规划」表示重新定位后的下一阶段目标，未实现。

### 2.1 微信通道接入（已实现）

- **单一入口**：`interfaces/wechat/WechatController`（含 `WechatCallbackController`），路径 `/wechat/callback`；在 `SecurityConfig` 中 `permitAll()`，由 `WechatSignatureVerifier` 执行平台验签。
- **验签与去重**：sha1 字典序签名；±300s 时间窗；同 `MsgId` 幂等去重（Redis `dedup:msg:{msgId}`）。
- **消息路由**：`MessageDispatcher` 单一入口 + 类型参数化路由（text/image/voice/location/event 策略族）。
- **先回执后推送**：回调 ≤700ms 内返回回执，终态经有界线程池异步推送（BR-06）。
- **token 管理**：缓存前缀分域 + 提前过期 + 分布式锁 + 单次失效重试（SRS 9.4.6）。
- **Mock 与真实通道复用同一 Controller 与同一段验签代码**。

### 2.2 对话与模型中枢（已实现）

- **Agent Loop**：`AgentOrchestrator`（`AgentOrchestratorImpl`）执行多轮工具编排，受 SC-01~SC-05 状态机硬约束（轮次 / 并发 / 超时）。
- **分发门控**：`MessageDispatcher` 分发前经 `UserStatusGate` 拦截禁用用户（不触发 LLM）。
- **模型调用**：经 `LlmClient` 抽象；`LlmClientRouter` 运行时多 provider 路由（见 2.10）。
- **上下文**：`ContextStore` 端口 + token 裁剪，保持 tool/assistant 消息配对不变式（SRS 9.4.4）。
- **实时观测（FR-08）**：编排过程事件经事件总线推送至 SSE 通道 `/api/sse/console`（详见 2.12）。
- **系统人设**：预置"衔光"人格，零配置。

### 2.3 AI 工具（Function Calling）（已实现）

工具位于 `domain/tool/impl`，统一实现 `Tool` 接口（`name()` / `description()` / `parametersSchema()` / `execute()`，另有 `idempotent()` / `critical()` / `readOnly()` 与三件自述元数据），由 `ToolRegistry` 构造器注入后**注册即暴露**进 LLM 函数 Schema。当前内置 6 个：

| 工具类 | 能力 | 备注 |
|---|---|---|
| `RecognizeImageTool` | 图片视觉识别（FR-10） | 语义 `recognize_image` |
| `SynthesizeVoiceTool` | 语音生成与发送（FR-11） | 语义 `synthesize_voice` |
| `QueryExpressTool` | 快递查询（FR-12） | 语义 `query_express` |
| `PlanRouteTool` | 导航服务（FR-13） | 语义 `plan_route` |
| `ManagePetProfileTool` | 宠物档案管理（FR-14） | 声明 `readOnly()` 支持回放护栏 |
| `QueryWeatherTool` | 天气查询（FR-23 **示例插件**） | 自包含样例数据集、结果标注 `data_source=mock-demo`；**默认由 `orchestration.disabled-tools` 灰度关闭**（V1.0.14） |

- 工具调用全程审计（`log_tool_call`，`call_seq` 递增）。
- 外部集成类工具为衔光自主实现，作为执行循环内部能力，不对外开放为市场。
- 「新增工具不改对话引擎」的契约、派生口径的反查方式与启动期 Fail-Fast 见 §2.24。


### 2.4 反幻觉与一致性校验（已实现）

- **执行一致性校验**：`ConsistencyChecker`（`RuleBasedConsistencyChecker`）+ `ActionClaimExtractor`——当模型声称"已完成某动作"但无对应工具执行记录时，判定 `CLAIM_UNSUPPORTED` 并拦截（FR-09）。判定词表**不在此类里硬编码**：由每个工具 `claimKeywords()` 自述、`ToolRegistry` 汇总反查（FR-23，见 §2.24）。
- **内容安全 Fail-Closed**：`ContentSafetyService` 词库不可用时按 Fail-Closed 处理。
- **兜底文案**：模型不可用或越界时返回受控兜底话术（可由后台热改）。
- **目标**：执行性幻觉对外泄漏 = 0（迭代 2 已构造 ≥20 泄漏场景验证，全部拦截留痕）。

### 2.5 审计与安全基线（已实现）

- 全量敏感操作写入 `log_audit`（增删改、用户启停、配置变更、回放等），前后值留痕（`before`/`after`）。
- 越权访问返回 403 并落痕；出参统一脱敏（`interfaces/assembler`，手机号 / OpenId）。

### 2.6 限流与成本预算（已实现）

- `RateLimitDecision` 枚举化决策；用户 / IP 双令牌桶 + 白名单豁免 + Fail-Open（FR-20）。
- `CostBudgetServiceImpl` 成本预算控制；限流事件落 `log_rate_limit`。
- 设计约束：多构造器 Bean 须显式标 `@Autowired`（见 6.2）。

### 2.7 数据保留与归档（已实现）

- 180 天清理策略；细粒度删除与匿名化（FR-19）。
- `DataRetentionServiceImpl` 实现清理；保留索引覆盖 `wx_message` / `wx_session` / `log_tool_call` / `biz_pet_profile` 常用检索列。

### 2.8 在线配置热生效（已实现）

- `DynamicConfigService`（接口）+ `DynamicConfigServiceImpl`（三级缓存读取 + 类型解析 + 降级回退，永不抛异常）。
- `ConfigAdminService`：类型校验 → 落库 → 缓存失效 → 审计；非法值被拒（400）；密钥落审计时遮罩。
- 编排器、兜底文案、strict-mode、fail-closed 等改为运行时读取，免重启生效（FR-18）。

### 2.9 回放调试台（已实现）

- `ReplayService`：工具调用干跑重放 + 一致性复现（A-2）。
- 工具声明 `readOnly()`（默认 false）；非只读工具重放时被拦（`skipped=true, skipReason=NOT_READ_ONLY`）。
- 端点：`POST /api/tool-logs/{id}/replay` 与 `/replay`；前端 `views/tool-log/replay.vue`（权限码 `toolLog:replay`）。

### 2.10 多模型路由（已实现）

- `LlmClientRouter`（`@Primary`）：按运行时 `llm.provider` 路由；目标实现未装配则回退并 WARN。
- `provider()` 汇报真实生效者，便于排查"实际用了哪个模型"（B-2）。

### 2.11 管家设置后台（已实现，转型）

- 原"管理后台"轻量化为"管家设置"：用户档案维护、在线配置编辑、回放台。
- 权限码集中（`config:*` / `user:*` / `toolLog:replay`）；路由 meta 与守卫同名同源。
- 不作为"搭建控制台"——衔光预置人格、零配置，不提供人设 / 音色 / 知识库配置界面。

### 2.12 流式输出与实时观测（FR-08，已实现）

- `ConsoleController` 暴露 SSE 通道 `GET /api/sse/console`；`ConsoleEventBuffer` 维护**环形缓冲（上限 100）**，支持 `Last-Event-ID` 断线续传与心跳保活。
- 编排过程中的事件经 `ConsoleEventRelay`（`@EventListener`）投递；鉴权支持 `JwtAuthenticationFilter` 的 `?token=` 查询参数回落（`EventSource` 无法自定义 Header）。
- 前端 `utils/sse.ts` 封装自动重连；提供管理后台实时观测台。
- **局限（如实标注）**：本轮的 `message_delta` 为**终态一次性推送**（用于观测）；真 token 级流式需 `LlmClient.stream()` 重构，列入后续迭代。

### 2.13 会话与工具调用监控（FR-17，已实现）

- `MonitorService`：六类**只读聚合**（概览 / 趋势 / 成功率 / 意图分布 / 延迟 / 降级），全部走现有落表数据，不新增写路径。
- `MonitorController` 暴露 `/api/monitor/*`；前端监控页（概览 / 会话 / 工具日志）用 ECharts 渲染四图，独立分包。
- **局限（如实标注）**：意图分布以「工具调用归因」近似（系统未持久化 LLM 意图域），未新增 `wx_message.intent` 列。

### 2.14 降级与拦截看板（A-3，已实现）

- 呈现四项质量度量：**降级触发率**、**四层异常分布**、**执行性幻觉拦截计数**、**对外泄漏 KPI**（后者恒 0，因发送前即拦截）。
- 降级率 = `status ∈ {2 降级, 3 超时}` / 总量；幻觉拦截以编排器一致性拦截处补写 `log_audit`（`reg_type=SAFETY`）为计数源。
- **四层分布的数据来源（迭代 4 W1 起为两表合并）**：
  - **L3 / L4**：`log_tool_call.error_type` 归类（该表天然只承载工具层 + SAFETY 审计的 L4）；
  - **L1 / L2**：`log_anomaly_event`（V1.0.10）按**显式 `layer` 列**直接聚合——埋点覆盖 SRS 2.3.5 的
    接入层判据（`SIGNATURE_INVALID` / `TIMESTAMP_OUT_OF_WINDOW` / `MSG_DUPLICATED` / `UNKNOWN_MSG_TYPE`）
    与认知层判据（`LLM_TIMEOUT` / `LLM_UNAVAILABLE` / `LLM_INVALID_OUTPUT`，取 `LlmException#errorType()`）。
  - 埋点位置：`WechatCallbackController`（验签 + 幂等）、`MessageDispatcher`（未知类型回落）、
    `AgentOrchestratorImpl`（两处 `catch (LlmException)` 分支，经 `AnomalyNotice` 事件解耦，不新增构造器依赖）。
- **仍存局限（如实标注）**：① L2 的「意图置信度不足」判据走的是 `LlmIntentClassifier → IntentResult.unknown()`
  的**正常追问澄清路径**（BR-08），非异常分支，故未计入 L2 埋点，看板上 L2 仅覆盖 LLM 调用失败三类；
  ② 埋点为 best-effort（写库失败仅 WARN），极端故障下计数可能偏低而非虚高；③ `log_anomaly_event` 尚未纳入
  180 天留存清理（与 `log_audit` / `log_rate_limit` / `log_orchestration_trace` 同状态，见 §7.9）。

### 2.15 档案变更留痕（A-4，已实现）

- 档案写操作记录**字段级 before/after diff**，形成可查询的变更历史；复用 `log_audit`（`reg_type=PROFILE`），不新增表。
- `PetProfileServiceImpl.audit()` 以 `normalize` 白名单将 camelCase 入参映射为 snake_case 存量字段名，并剔除系统字段，避免 diff 噪声。
- `ProfileHistoryController` 暴露 `GET /api/profiles/history` 供后台查询。

### 2.16 超时预算时序瀑布（A-5，已实现）

- 编排器以 `System.nanoTime()` 记链路起点，为**每轮 LLM 调用**（`LLM_ROUND`）与**每次工具调用**（`TOOL`）记录 span（相对偏移 / 耗时 / 状态）。
- 链路结束组装 `OrchestrationTracedEvent`（含 `totalMs` / `totalBudgetMs` / `rounds` / `exceededBudget`）→ 落 `log_orchestration_trace`（V1.0.9）→ `GET /api/monitor/trace/{traceId}` → 前端甘特瀑布图（红色虚线标注 25s 预算线）。
- 使 SRS 4.4.2 的 SC-01~SC-05（5 轮 / 3 并行 / 25s 总预算）从文档条款变为**可核查事实**。

### 2.17 任务型多步会话（FR-24，已实现）

- `TaskSessionService`：追问时建任务 → 用户回复**优先槽位填充**（`SlotFiller`，确定性规则、不调 LLM）而非全量重识别 → 槽位齐备 `resume` 恢复原任务执行。
- **追问上限 3 次（BR-33）**：连续 3 次无法填充即放弃，并给出明确说明，不出现第 4 次追问；多任务并存**上限 2 个**（栈式）。
- **超时 10 分钟**无交互即结束并清理；话题明显切换则中断并提示"已暂停之前的操作"；Redis 故障视为任务取消。
- **存储分离（BR-34）**：任务上下文 `{taskType, requiredSlots, filledSlots, expireAt}` 落 `wx_session.task_context`（已存在列，无需迁移），活跃态存 Redis `task:{openid}`（TTL 10min），与 LLM 上下文键 `conv:{openid}` **完全分离**。
- 状态同步至 `Session.state=TASKING` 供后台观测；`TaskController` 暴露 `GET /api/tasks/{sessionId}` 与 `POST /api/tasks/{sessionId}/abandon`，**不暴露用户侧写接口**。

### 2.18 环境自检（Startup Doctor，A-1，已实现）

- `StartupDoctor`（`StartupDoctorImpl`）逐项校验 **DB / Redis / 微信 / LLM / TTS / 物流 / 地图** 的连通性、运行模式（Mock/Real）与配置校验结论，输出结构化体检报告 `DoctorReport`。
- 端点 `GET /api/doctor`（`DoctorController`，`@PreAuthorize("isAuthenticated()")`，任意已认证角色可用）；**体检过程不抛异常，故该端点永不为 5xx**。
- 与 `PlaceholderConfigValidator`（G3 占位符校验）配合，直接对抗前身"配置齐全但链路为空"的失败模式。
- 前端在仪表盘（`views/dashboard`）呈现体检结果。

### 2.19 个人状态库（W6，地基已落地）

- **定位**：跨会话持续生长的**个人知识**（人 / 地 / 物 / 偏好 / 惯例 / 事实），带溯源，是 [2.20 事件与触发引擎](#220-事件与触发引擎规划迭代-4) 与 [2.21 主动执行与承诺闭环](#221-主动执行与承诺闭环规划迭代-4) 两层的数据地基。
- **不是上传式 RAG**：条目由**每轮成功对话结束后自动抽取**产生（生长式），不依赖人工灌库；抽取失败或无产出时链路行为与改动前完全一致。
- **单表落点**：`biz_memory_item`（V1.0.11）。同一 `(openid, kind, name)` 为一条生效事实，冲突时**新值覆盖旧值**（旧行转 `SUPERSEDED` 并保留 `supersedes_id` 链），重复提及则 `hit_count + 1 / last_seen_at` 刷新——覆盖与强化皆可回溯。
  - `kind`：`PERSON / PLACE / THING / PREFERENCE / HABIT / FACT`；**承诺（COMMITMENT）不在本表**，与 `biz_commitment` 一并留待 W8（无触达通道的承诺是空头承诺）。
  - `origin`：`AUTO_EXTRACT`（已用）/ `TOOL`（**预留未启用**，工具主动写入待 W8 的 `remember`）。
  - **溯源**：`source_session_id` + `source_trace_id` + `extractor` + `confidence`。溯源指向会话与编排链路而非微信 MsgId——编排层看不到接入层 MsgId，且 `wx_message` 无 `trace_id` 列，故不设 `source_msg_id`（不留填不上值的列）。
- **生长管道**：编排器成功产出终态回复后发布 `MemoryGrowthNotice` → `MemoryGrowthListener`（自带**单线程 + 64 队列**的守护线程池）→ `LlmMemoryExtractor`（约束式 JSON 抽取，5s 超时，token 计入 `CostBudgetService`）→ `MemoryStore.upsert`。队列满即丢弃并计数（`droppedCount()` 可查），**不背压、不阻塞用户链路**。
- **召回注入**：`MemoryRecallService.buildRecallBlock(openid)` 以一条 `system` 消息注入本轮上下文（自带上限：条数 + 字符数，越界即截断），块内首行明示「不得据此编造已执行的动作」，与 BR-04 / 一致性校验语义保持一致；召回失败**放行不阻塞**（fail-open）。
- **热配置（`sys_config`，V1.0.12 播种）**：`memory.growth.enabled`（**默认 false**）、`memory.growth.max-items`（默认 5）、`memory.recall.enabled`（默认 true）、`memory.recall.max-items`（8）、`memory.recall.max-chars`（600）。生长默认关闭是因为它按消息计费地增加一次模型调用（G-32/G-33「预留未启用」口径）；召回只多一次索引读，故默认开启。**只开召回不等于状态库在用**——库空则召回为空。
- **PII 归属（FR-19）**：条目是对话的**派生 PII**。`scope=CHAT/ALL` 的用户数据删除会一并物理清空该 openid 的条目；留存任务只按 180 天清理 `SUPERSEDED` 历史，**生效事实不自动过期**（退出只有两条路径：被新事实覆盖、或用户删除数据）；合规报告的基数分母含 `biz_memory_item`。
- **隔离**：所有读写以 `openid` 为强制条件（BR-07）；日志中的 openid 经 `MaskUtils` 脱敏（BR-21）。
- **代码落点**：`domain/memory`（`MemoryWrite` / `MemoryFact` / `MemoryStore` 端口）、`application/memory`（抽取 / 监听 / 召回）、`application/admin/MemoryAdminService` + `interfaces/admin/MemoryController` + `interfaces/dto/memory/MemoryItemVO`（W6-b 后台）、`infrastructure/persistence/{entity,mapper,repository}`、`common/enums/Memory{Kind,Status,Origin}`。
- **后台界面（W6-b，已实现）**：`GET /api/memories` 分页只读列表（过滤 `openid`/`kind`/`status`，按 `last_seen_at` 倒序，出参 openid 经 `MaskUtils` 脱敏，BR-21），SUPER_ADMIN / OPERATOR / AUDITOR 可读（BR-23 口径同监控，AC-E9）；`DELETE /api/memories/{id}` 为**人工纠错**入口，**仅 SUPER_ADMIN**，逻辑删除（置 `deleted_at`，与 `live_marker` 生成列一致：删除后 `(openid, kind, name)` 槽位释放、生长可重新写入），并写 `log_audit`（`reg_type=MEMORY`，前值快照仅含 kind/name/content/origin）；**不提供编辑**——修正语义由生长管道的「覆盖」承担，避免人工值与自动值争抢同一事实。前端为 `views/memory` 只读页（菜单「个人状态库」，删除按钮经 `v-permission` 仅 SUPER_ADMIN 可见，后端独立鉴权兜底）。
- **剩余缺口（如实标注）**：条目纠错只有「删除」没有「改错后保留」路径——误抽取删除后依赖下一轮对话重新生长；审计快照不含完整行（如 `source_trace_id`），如需全链路取证仍需查库。

### 2.20 事件与触发引擎（规划·迭代 4）

- 订阅微信事件 / 定时 tick / 状态变化，决策"何时主动行动"。
- 架构心脏：使衔光从 turn-driven 转为 event-driven，无需用户发消息即可行动。
- 依赖服务号 / 企业微信的主动触达能力（订阅号无主动推送）。

### 2.21 主动执行与承诺闭环（规划·迭代 4）

- 主动执行器：模板消息 / 订阅通知 / 群@ / 定时推送。
- 承诺闭环追踪："你说过 X" → 到点 / 逾期主动提醒与跟进。
- 垂直个人动词工具：`remember` / `remind` / `followUp` / `recall` / `delegate`。

---

### 2.22 合规报告自动化（B-5 / W4，已实现）

- **目标**：一键导出「数据留存与删除执行」合规自证报告（Markdown），聚合既有日志/业务表，**不引入新采集、不新增表**。
- **端点**：`GET /api/compliance/report`（`ComplianceController`），`@PreAuthorize("hasRole('SUPER_ADMIN')")`，响应 `Content-Type: text/markdown` + `Content-Disposition: attachment; filename=compliance-report-YYYYMMDD.md`。
- **报告六大主题**：① 数据保留策略与执行（FR-19①，保留天数取自 `DataRetentionService` 常量：消息/工具日志 180 天、软删档案宽限 30 天）；② 删除与匿名化执行（FR-19②，按范围 ALL/CHAT/PET 聚合、`anon_` 前缀计数）；③ 审计与运维；④ 工具调用与异常（含失败率）；⑤ 限流；⑥ 编排链路（预算超限/耗时分布）。
- **脱敏**：所有 openid 经 `MaskUtils.openid` 兜底脱敏（BR-21）；近期审计行仅透出脱敏后的 `target`，绝不携带 `before_value/after_value` 原始 PII。
- **前端入口（已补）**：`/compliance/export`（菜单「合规报告导出」）→ `views/compliance/export.vue`，按钮一键下载，文件名取服务端 `Content-Disposition`；权限码 `compliance:export` 仅授予 SUPER_ADMIN（与后端 `@PreAuthorize` 同口径），OPERATOR/AUDITOR 菜单不可见、深链由守卫回退 `/dashboard`。下载走原生 `fetch`（`services/compliance.api.ts`）——axios 单例的响应拦截器按 `ApiResponse` 拆包，不适用于文件流（与用户 CSV 导出同一套路）。
- **代码落点**：`application/compliance`（聚合 record + `ComplianceReportService` 端口）、`infrastructure/compliance/ComplianceReportServiceImpl`、`interfaces/admin/ComplianceController`、`interfaces/assembler/ComplianceReportAssembler`、`interfaces/dto/compliance/ComplianceReportVO`；单测 `ComplianceReportServiceImplTest`（5 例，Mockito 桩 9 个 Mapper）；前端 `services/compliance.api.ts` + `views/compliance/export.vue` + `config/permissions.ts` / `router/index.ts` / `layouts/MainLayout.vue`。
- **证据等级**：A 级（代码 + 单测 + 全量 `mvn clean verify` **359 测试全绿**、Checkstyle 0 违例、SpotBugs 通过；后端端点真实 200 见 §7.8 / §7.10，前端入口浏览器实证见 §7.12）。
- **复核更正**：本模块首次落地时 `ComplianceReportAssembler` 存在**字符字面量语法错误**（`append('\n\n')`），主源码根本编译不过；此前「e2e 8/8 走查证明含合规代码可编译」的推断不成立（e2e 跑的是不含该模块的旧 jar），且 `ComplianceReportServiceImplTest` 因缺 import 与缺 MyBatis-Plus `TableInfo` 缓存从未成功运行。以上均已在提交前修复并实跑通过（见 §7.8）。

---

### 2.23 灰度开关与熔断回滚（FR-22 / W2，已实现）

- **定位**：灰度是**叠加在功能主开关之上的第二道闸门**——主开关关闭时灰度不参与判断。因此新增灰度项**不改变任何既有行为**：`gray.<功能>.percent` 播种为 **100**（等同灰度上线前），"放量"这个动作是管理员把它调成 1/5/20，而不是"打开灰度"。**置 0 是一刀切**（白名单同时失效），否则 FR-22 异常流「回滚后行为恢复到变更前」（AC③）无法成立。
- **配置键（`sys_config`，V1.0.13 播种 7 项，category=`gray`）**：`gray.memory_growth.percent`（0~100）、`gray.memory_growth.whitelist`（逗号分隔 openid，仅比例 > 0 时参与）、`gray.breaker.enabled`（默认 true）、`gray.breaker.window-minutes`（5）、`gray.breaker.min-samples`（20）、`gray.breaker.error-rate-percent`（30）、`gray.breaker.p95-ms`（**0 = 该判据不启用**）。
- **分流判定顺序**（`GrayReleaseService.decide`）：openid 缺失 → `MISSING_OPENID` 不命中 → 比例 ≤ 0 → `GRAY_OFF` → 比例 ≥ 100 → `FULL_ROLLOUT` → 白名单命中 → `WHITELIST` → 稳定分桶 `bucket < percent` → `PERCENT` / `NOT_IN_GRAY`。**分桶用 SHA-256(`代号#openid`) 前 4 字节 mod 100**，不用 `String#hashCode()`：前者跨 JVM 稳定，是「同一用户恒定同一桶」（AC① 抽样 ±2% 的前提）与「改比例只放量、不洗牌」（单调放量）的最低实现；盐值取功能代号，避免同一批用户在不同功能上完全同桶。配置越界时读取侧按 0 处理并告警（写入侧已先拦）。
- **端点**（`interfaces/admin/GrayController`）：`GET /api/gray/status`（`hasAnyRole('SUPER_ADMIN','OPERATOR','AUDITOR')`，BR-23 只读口径）聚合各功能比例 / `whitelistSize` / `closed` + 熔断阈值（含 `checkIntervalSecond=30`）+ 近窗健康度实测；`GET /api/gray/preview?feature=&openid=`（同口径，代号不存在返回 10002）用于自证「同一入参必得同一结果」，出参 openid 经 `MaskUtils` 脱敏；`POST /api/gray/rollback`（`hasRole('SUPER_ADMIN')`，reason 必填 BR-25）一键回滚。**改比例与白名单仍走 `PUT /api/configs`**——刻意不新增第二套写入口，从而天然继承 FR-18 的值域校验、缓存失效与逐项审计。
- **写入校验（异常流 2a）**：`ConfigAdminService` 对 `gray.*.percent` 后缀键强制 0~100，越界（101 / -1）拒绝并回 `10002`；`gray.*` 下未播种的键按「配置项不存在」拒绝，杜绝凭空造灰度项。
- **熔断回滚**（`GrayCircuitBreaker`，`@Scheduled` 每 30s，单次评估只跑两条聚合查询）：判据为「窗口内 **L2~L4** 异常数 / 链路轮次」超错误率阈值，或链路 P95 时延超阈值（阈值 0 视为不启用），且窗口轮次 ≥ `min-samples`；任一越界即把**全部**在放量功能比例置 0，30s 内热生效（**AC②「≤60s」满足**）。分子**排除 L1**（接入层验签失败/重投多由外部探测造成，计入会让一次回调风暴误触发全局回滚）；**样本不足不回滚**（低峰期一条异常不应清零放量）。告警分级：真实回滚打 ERROR；越界窗口未过但已无在放量功能时降为 WARN，避免每 30s 一条重复告警把真告警淹没。
- **回滚复用而非另建**（`GrayRollbackService` → `ConfigAdminService.update`）：于是一次回滚自动获得值校验、`configCacheService.invalidate`（免重启）、逐项 `CONFIG_UPDATE` 审计，并额外补一行 `reg_type=GRAY / action=ROLLBACK` 汇总审计（记录回滚前明细 `代号=原比例` 与触发原因），使「因何回滚」与「改了哪些键」同行可比对；系统自动回滚时 `admin_id`/`ip` 为 NULL，与人工回滚可区分。**白名单不在回滚范围内**（置 0 后已不生效，保留名单值便于事后仍可看出放量计划）。
- **接线现状**：`memory.growth.enabled`（功能主开关，W6）之外再叠加 `GrayReleaseService.isHit(MEMORY_GROWTH, openid)`——生长按用户小步放量，因为它是"每轮多一次模型调用"的成本型功能（G-32/G-33 口径）。受灰度治理的功能清单用枚举 `GrayFeature` 而非运行时注册表：**新增灰度项须过评审**（新增即意味着熔断回滚会覆盖它）。
- **口径的边界（如实声明）**：① 异常与链路表只存**脱敏** openid（BR-21），异常未按功能归因，因此熔断是**全局健康度杀开关**而非"谁坏了关谁"——这与 BR-31「宁可误关不可漏关」一致，但不是细粒度归因，后者需要按功能埋点；② FR-22 基本流的**多 Prompt A/B 并行与效果对比看板**未实现（见 §7.13 未取得证据）；③ 后台**灰度观测台/放量控制台前端页**尚未做（当前以 `PUT /api/configs` 写、`GET /api/gray/status` 读），排在 W2-b。
- **代码落点**：`application/gray/{GrayFeature,GrayReleaseService,GrayHealthMetrics,GrayHealthSnapshot,GrayCircuitBreaker,GrayRollbackService}`、`infrastructure/gray/GrayHealthMetricsImpl`、`interfaces/admin/GrayController`、`interfaces/dto/gray/{GrayStatusVO,GrayPreviewVO,GrayRollbackRequest,GrayRollbackVO}`、`MaskingAssembler.toGrayPreviewVO`、`ConfigKeys`（7 键）、`ConfigAdminService`（灰度键值域校验）、`resources/db/migration/V1.0.13__seed_gray_config.sql`；测试 5 类 30 例（`gray/GrayReleaseServiceTest` 9 / `gray/GrayCircuitBreakerTest` 7 / `gray/GrayRollbackServiceTest` 4 / `interfaces/admin/GrayControllerTest` 5 / `infrastructure/gray/GrayHealthMetricsImplTest` 5）+ 既有用例扩充（`ConfigAdminServiceTest` 值域 2 例、`MemoryGrowthListenerTest` 灰度回滚停生长 1 例）。**无新表**（只读 `log_orchestration_trace` + `log_anomaly_event`，写入 `sys_config` + `log_audit`）。
- **证据等级**：A 级（代码 + 单测 + 全量 `mvn clean verify` **457 测试全绿**、Checkstyle 0 违例、SpotBugs 通过；干净库 `clawbot_w2` 冷启动与 AC②③ / 异常流 2a / RBAC / 脱敏逐项 HTTP + 库内实证见 §7.13——其中 **AC① 的 ±2% 抽样只有单测实证**，运行期实证的是桶号稳定与放量不洗牌）。

### 2.24 工具插件化扩展接口（FR-23 / W3，已实现）

- **要解决的问题**：工具名一旦成为**封闭枚举**，"插件化"就是假的。W3 前有三处按工具名硬编码的派生口径——反幻觉判定词表（`ActionClaimExtractor.SEMANTIC_KEYWORDS`）、监控看板归因域（`MonitorService.mapToolToIntent`）、任务会话意图（`TaskSessionServiceImpl.TASK_INTENTS`）。新增一个工具要同时改这三个业务模块，等于没有扩展接口。
- **契约（`domain/tool/Tool`）**：在原有 `name()/description()/parametersSchema()/execute()` + `idempotent()/critical()/readOnly()` 之外，新增三件**默认方法形式的自述元数据**，全部向后兼容（既有工具不实现也能启动，BR-32）：
  - `Set<String> claimKeywords()`（默认**空集**）——本工具"声称完成"时的判定词。默认空集是**安全侧**选择：漏声明只会少一道校验，绝不会反过来把正常回复误判成幻觉。
  - `String monitorDomain()`（默认 **`chat`**）——看板归因域。默认落在既有的"未归因"桶，**不凭空造域**。
  - `IntentType taskIntent()`（默认 **`null`**）——任务会话的话题切换意图。`null` = 该工具不参与话题切换判定（保守默认）。
- **反查唯一入口（`ToolRegistry`）**：`claimKeywordsOf(name)` / `claimKeywordUniverse()` / `monitorDomainOf(name)` / `taskIntentOf(name)`。三处消费方**只按名反查注册表**，不再持有工具名的副本；未注册名一律回落契约默认值。
- **启动期 Fail-Fast（SRS 异常流 1a / 1b）**：`ToolRegistry` 构造时逐项校验，任一违规**直接阻止启动**并给出可定位到实现类的中文说明：
  - Schema 为空 / 无法解析 / 根节点缺 `type` / 某参数缺 `type` / `required` 引用未声明参数 → `工具参数 Schema 非法，启动终止: {name}（{实现类}）→ {逐条违规}`。
  - 参数名重复 → 由 Jackson `StreamReadFeature.STRICT_DUPLICATE_DETECTION` 在解析期检出（默认行为是静默保留后者，正是 SRS 要点名的隐患）。
  - 工具名冲突 → 提示**冲突双方实现类全名**；工具名不合法（非小写下划线）→ 同样附实现类。
- **零侵入的实证载体**：`QueryWeatherTool` 是刻意留下的"活样本插件"——新增它时，**对话引擎 `AgentOrchestratorImpl` 与 `resources/prompt/**` 零改动**（diff 为证，AC①），工具发现、参数下发、调用执行、审计落表、看板归因全部自动到位。需要澄清的是：本轮为了让这种零改动成立，先把三处派生口径的消费方（`ActionClaimExtractor` / `MonitorService` / `TaskSessionServiceImpl`）从「按工具名硬编码」改成了「注册表反查」——**这是一次性成本**，此后新工具不再触碰它们。接入面因此收敛为「1 个类 + 1 条配置」。
- **那条"配置"= FR-18 的工具开关**：`sys_config` 的 `orchestration.disabled-tools` 加一个名字即可（V1.0.14 把 `query_weather` 默认加入该清单 = **暗启动**）。刻意复用 FR-18 的开关与热生效，不新增第二套写入口；代价是"默认禁用"要靠迁移播种，而不是代码里的静态兜底。
- **口径的边界（如实声明）**：
  1. `IntentType` 仍是封闭枚举，**不占既有意图的插件只能吃 `taskIntent()=null`** 的保守默认（不参与话题切换判定）。彻底解开需要把意图本身开放注册，属后续工作。
  2. "1 个类"是字面成立的，但**让模型真的去调用它**还需要 Mock 脚本数据（`mock/llm-scripts.yml` 的 2 条规则）——那是测试替身，不是引擎或 Prompt；接真实模型时由模型自行选择。
  3. SRS FR-23 的**备选流 2a（工具分组 / 按角色授权）与 4a（工具级限流）本轮未实现**（见 §7.7 未实现项），当前只有全局 `orchestration.disabled-tools` 一刀切。
- **代码落点**：`domain/tool/{Tool,ToolRegistry,JsonSchema}`、`domain/tool/impl/*`（五件既有工具补自述 + 新增 `QueryWeatherTool`）、`application/safety/ActionClaimExtractor`、`application/admin/MonitorService`、`application/task/TaskSessionServiceImpl`、`resources/db/migration/V1.0.14__dark_launch_weather_tool.sql`、`resources/mock/llm-scripts.yml`；测试新增 4 类（`domain/tool/ToolRegistryTest` 11 例、`domain/tool/JsonSchemaTest` 8 例、`application/safety/ActionClaimExtractorTest` 4 例、`verification/QueryWeatherToolTest` 5 例）+ 夹具 `support/ToolRegistries`（离线构造生产工具集，避免测试另抄一份词表）。
- **证据等级**：A 级（代码 + 单测 + 全量 `mvn clean verify` **485 测试全绿**；干净库 `clawbot_w3` 冷启动、AC② 非法 Schema 真实启动失败、AC③ 日志 + `log_tool_call` + 看板归因域实证见 §7.14）。

---

## 第 3 章 技术架构

### 3.1 总体架构

```
                  ┌──────────────────────────────────────────┐
                  │           微信通道接入层 (已实现)            │
                  │   公众号/企业微信 · 消息·媒体·事件·回调       │
                  └───────────────────┬──────────────────────┘
                                      │ 事件流(event)
      ┌───────────────────────────────┼───────────────────────────────┐
      │  事件与触发引擎 (规划·迭代4, 架构心脏)                          │
      │  - 订阅微信事件/定时tick/状态变化                               │
      │  - 决策：何时主动行动（无需用户发消息）                         │
      └───────────────┬───────────────────────────┬───────────────────┘
                      │                           │
        ┌─────────────▼──────────┐    ┌────────────▼────────────┐
        │ 个人状态库 (规划·迭代4)   │    │ 主动执行器 (规划·迭代4)    │
        │ - 人/地/物 实体          │    │ - 模板消息/订阅通知/群@    │
        │ - 承诺/偏好/惯例          │    │ - 创建提醒/追踪闭环        │
        │ - 带溯源的事实(生长式)    │    │ - 无需对话即可行动         │
        └─────────────┬──────────┘    └────────────┬────────────┘
                      │                           │
                      └─────────────┬─────────────┘
                                    ▼
                  ┌─────────────────────────────────────┐
                  │  认知中枢 (LLM) + 工具注册表 (已实现)   │
                  │  反幻觉 · 一致性校验 · 审计 (已实现)     │
                  └─────────────────────────────────────┘
                                    │
                  ┌─────────────────────────────────────┐
                  │  管家设置后台 (已实现, 轻量化转型)        │
                  └─────────────────────────────────────┘
```

当前已实现部分 = 微信通道接入 + 认知中枢 + 工具注册表 + 审计/限流/归档/配置/回放/多模型 + 管家设置后台。规划三层（个人状态库 / 事件触发 / 主动执行）为其上的执行体升级，其中**个人状态库的地基（表 + 生长管道 + 召回注入 + 溯源）已随 W6 落地**（见 [2.19](#219-个人状态库w6地基已落地)），事件触发与主动执行仍为规划。

### 3.2 后端分层与依赖铁律

后端严格遵循五层包结构 `interfaces → application → domain → infrastructure → common`，依赖方向**自上而下单向**，禁止反向依赖与跨层跳跃。

```
com.lumensteward.clawbot
├── interfaces/                 # 接口层：Controller / DTO / Assembler（统一出参脱敏）
│   ├── wechat/                 # WechatCallbackController（单一入口 /wechat/callback）
│   ├── admin/                  # 后台 REST 控制器（含 Console / Monitor / ProfileHistory / Task）
│   └── dto/                    # 出参 VO（分域子包，如 dto/monitor、dto/session）
├── application/                # 应用层：用例编排（无业务规则）
│   ├── dispatcher/             # 消息路由（策略族）
│   ├── orchestrator/           # Agent Loop 编排（含 model/ span）与事件发布
│   ├── console/                # SSE 事件总线（ConsoleEventRelay / ConsoleEventBuffer / OrchestrationTraceListener）
│   ├── task/                   # 任务会话与槽位填充（TaskSessionService / SlotFiller）
│   ├── context/                # 上下文服务 + token 裁剪
│   ├── safety/                 # 内容安全 + 执行一致性校验
│   ├── fallback/               # 降级文案与降级矩阵
│   ├── admin/                  # UserStatusGate / UserAdminService / ReplayService / ConfigAdminService / MonitorService
│   ├── gray/                   # 灰度分流与熔断回滚（GrayReleaseService / GrayCircuitBreaker / GrayRollbackService / GrayFeature）
│   ├── config/                 # DynamicConfigService / ConfigKeys
│   └── wechat/                 # 先回执后推送
├── domain/                     # 领域层：业务规则与端口（不含框架依赖）
│   ├── tool/                   # Tool 契约（含自述元数据）/ ToolRegistry（启动校验 + 派生口径反查）/ JsonSchema（含 impl/）
│   ├── task/                   # TaskStore 端口（任务活跃态）
│   ├── model/                  # 领域模型（TaskContext 等）
│   ├── intent/                 # 意图模型与分类端口
│   ├── service/                # 业务能力端口
│   └── port/                   # 领域端口（logistics/map/tts/llm 视觉上提）
├── infrastructure/             # 基础设施层
│   ├── persistence/            # entity / mapper / repository 实现（含 service/）
│   ├── cache/                  # ContextStore / 去重 / 限流 / 黑名单 / 配置缓存 / 任务活跃态（RedisTaskStore）
│   ├── client/                 # llm（含 LlmClientRouter）/ wechat / logistics / map / tts（SPI 实现）
│   ├── ratelimit/              # RateLimitDecision / CostBudgetService
│   ├── retention/              # DataRetentionService
│   ├── config/                 # 配置类收敛 + 属性类（含 DynamicConfigServiceImpl）
│   ├── bootstrap/              # Startup Doctor / 占位符校验 / Admin 引导
│   └── observability/          # traceId Filter / MDC / 脱敏
└── common/                     # 通用：Result / 错误码 / 异常 / 枚举 / 工具
```

**依赖方向铁律**

| 从 | 到 | 允许 | 说明 |
|---|---|---|---|
| interfaces | application / domain / common | ✅ | Controller 只做协议转换与鉴权注解，不含业务规则 |
| application | domain / common | ✅ | 编排器调用领域端口；**不得**直接引用 Mapper |
| domain | common | ✅ | 领域层定义端口接口，不含框架依赖 |
| infrastructure | domain / common | ✅ | 实现领域端口、提供 SPI 实现 |
| 反向依赖 / 跨层跳跃 | — | ❌ | 任何场景禁止 |

> 迭代 2 结构变更要点：`infrastructure/client/{logistics,map,tts}` 与 LLM 视觉接口 **上移到 `domain/port/`**，基础设施 Mock/Real 实现改为实现领域端口，保证 `domain` 不依赖 `infrastructure`。

> 迭代 4（W3）结构变更要点：原先散落在应用层的三处「按工具名硬编码」的派生口径（反幻觉词表 / 看板归因域 / 任务会话意图）**下沉为工具自述 + `ToolRegistry` 反查**（§2.24）。方向仍合法：`application → domain`，且 `MonitorService` / `ActionClaimExtractor` / `TaskSessionServiceImpl` 由此只依赖领域注册表，不再需要为每个新工具改代码。

### 3.3 前端结构

```
frontend/src
├── router/index.ts             # 路由 + meta 守卫（同名同源）
├── config/permissions.ts       # 权限码/角色/菜单映射
├── stores/                     # auth / permission / config / toast / ui
├── utils/api.ts                # axios 三段式单例封装
├── utils/sse.ts                # EventSource 封装 + 自动重连（FR-08）
├── components/charts/          # EChartBase 薄封装 + 趋势/意图/延迟/成功率/瀑布 图
├── types/                      # api/auth/user/session/monitor/memory/console/toolLog/config/audit…
├── services/*.api.ts           # 一域一文件（含 monitor / task / profile / memory）
├── layouts/MainLayout.vue
└── views/                      # login / dashboard / user(+detail) / session(+detail) / monitor / tool-log(+replay) / config / audit-log / profile / memory / compliance
```

### 3.4 通信设计：微信回调与消息

- 微信侧以 HTTP 回调推送消息 / 事件；`WechatCallbackController` 校验签名后路由。
- 出站通过微信消息 API 主动回复；主动触达（规划）走模板消息 / 订阅通知 API。
- 后台管理走 REST + 统一 `code`/HTTP 状态分层；`traceId` 由入口 Filter 生成写入 MDC。

### 3.5 数据模型

当前 12 张核心表（分域前缀 `sys_` / `wx_` / `biz_` / `log_`），全部经 Flyway 增量迁移（V1.0.0 ~ **V1.0.14**）：

| 前缀 | 表 | 用途 |
|---|---|---|
| `sys_` | `sys_admin_user` / `sys_config` | 管理员、在线配置（含 V1.0.13 播种的 7 项 `gray.*` 灰度/熔断配置、V1.0.14 把示例工具 `query_weather` 加入 `orchestration.disabled-tools` 暗启动，**无新表**） |
| `wx_` | `wx_user` / `wx_session` / `wx_message` | 微信用户、会话、消息（`wx_session.task_context` 承载 FR-24 任务上下文） |
| `biz_` | `biz_pet_profile` / `biz_memory_item` | 业务档案（宠物）、个人状态库条目（W6，V1.0.11；派生 PII，见 §2.19） |
| `log_` | `log_audit` / `log_tool_call` / `log_rate_limit` / `log_orchestration_trace` / `log_anomaly_event` | 审计、工具调用、限流、链路时序（A-5，V1.0.9）、四层异常事件（A-3 的 L1/L2 埋点，W1，V1.0.10） |

约定（SRS 7.6）：审计字段 `created_at`/`updated_at`/`deleted_at` 须配套 `MetaObjectHandler`；字段 `COMMENT` 与 `utf8mb4`；索引 `uk_`/`idx_`；SQL 按域分文件 + 增量迁移。

### 3.6 外部服务集成（SPI 抽象）

| 端口 | 实现 | 说明 |
|---|---|---|
| `LlmClient` | Mock / OpenAiCompatible（经 `LlmClientRouter`） | 认知中枢 |
| `WechatTransport` | Mock / Real | 微信消息收发 |
| `LogisticsClient` / `MapClient` / `TtsClient` | Mock（Real 待 TODO-03/04） | 外部能力，注册为工具 |

- 无真实凭据时以 Mock 适配器跑通全链路；真实凭据到位后改配置切换。
- 全部集成独立实现，不依赖任何外部项目或共享模块。

### 3.7 鉴权与权限模型

- 管理后台：JWT + **三角色**（`AdminRole`：SUPER_ADMIN / OPERATOR / AUDITOR）；权限码集中、路由守卫同名同源。
- 微信侧：以 `OpenId` 标识终端用户；禁用用户由 `UserStatusGate` 分发前拦截。
- 越权统一 403 + 审计留痕。

### 3.8 关键运行时机制

| 机制 | 说明 |
|---|---|
| Agent Loop | 多轮工具编排受轮次/并发/超时硬约束（SC-01~SC-05）；未注册工具判 NOT_EXECUTED |
| 先回执后推送 | 回调 ≤700ms 回执，终态异步推送（BR-06） |
| 执行一致性校验 | 声称成功但无执行记录 → CLAIM_UNSUPPORTED 拦截 |
| 内容安全 Fail-Closed | 词库不可用时按 Fail-Closed 处理 |
| 配置热生效 | 运行时读取，改后免重启 |
| 回放护栏 | 非只读工具干跑重放时跳过 |
| 多模型路由 | 运行时 `llm.provider` 路由，缺实现回退 + 告警 |
| SSE 观测通道 | 编排事件入环形缓冲（100），`Last-Event-ID` 续传 + 心跳；`?token=` 回落鉴权（FR-08） |
| 链路时序埋点 | `System.nanoTime()` 记起点，逐 span 记录并落表；超预算标 `exceededBudget`（A-5） |
| 任务会话续接 | 追问建任务 → 槽位填充优先于全量重识别；≤3 次放弃、10min 超时清理（FR-24） |
| 限流 Fail-Open | 限流组件异常时放行，避免误伤正常请求（FR-20） |
| 数据保留清理 | 180 天定时清理 + 细粒度删除 + 匿名化（FR-19）；状态库仅清理已覆盖历史，生效事实不自动过期（W6） |
| 状态库生长 / 召回 | 每轮成功对话后异步抽取（独立线程池，队列满即丢弃）；召回以一条 system 块注入本轮上下文，失败放行（W6，见 §2.19） |

---

## 第 4 章 开发规范

详细规范以 SRS 第 7~9 章与附录 G 为权威基线，摘要如下：

- **建表与命名（SRS 7.6）**：表名前缀分域、索引 `uk_`/`idx_`、审计字段配套填充处理器、`utf8mb4`、增量迁移。
- **接口约定（SRS 8.2.1）**：`code` 与 HTTP 状态分层、入口 Filter 生成 `traceId` 写 MDC、分页契约全局唯一、统一脱敏。
- **错误码（SRS 8.5）**：分段等距、段内切块、段位不重叠、`70xxx+` 为扩展段，禁止复用第三方返回码。
- **后端（SRS 9.2）**：构造器注入、SLF4J 占位符、配置类收敛、分页拦截器与填充处理器必须注册、`OR` 分组包裹、异常与 HTTP 状态显式绑定。
- **前端（SRS 9.3）**：axios 三段式、API 一域一文件、权限集中、Pinia 拆分、类型全局唯一、`.env` 三件套、`@` 别名双端同步。
- **微信通道（SRS 9.4.6）**：token 缓存分域 + 提前过期 + 分布式锁 + 重试；XML↔DTO 双向（+XXE 防护）。
- **三道闸门**：范围闸门（3.5 范围冻结 + 分层单体）、证据闸门（诚实性声明 + 证据等级）、质量闸门（CI 门禁前移）。

---

## 第 5 章 部署流程

面向单拥有方自治部署。

### 5.1 依赖

| 依赖 | 服务名 | 端口 | 口令 | 说明 |
|---|---|---|---|---|
| MySQL | `MySQL84` | 3306 | `root / 1234`（本机实测） | 库 `clawbot`，Flyway 首次启动自动迁移 |
| Redis | `redis` | 6379 | 无口令 | 缓存 / 限流 / 去重 / 锁 |

> 本机 `DB_HOST` 用 `127.0.0.1` 而非 `localhost`，避免 Windows 下 mysql-connector 将 `localhost` 当作命名管道。开发阶段直接使用本机 MySQL / Redis（Docker 编排暂搁置）。

### 5.2 后端启动（local profile）

```bash
cd backend
SPRING_PROFILES_ACTIVE=local \
DB_HOST=127.0.0.1 DB_PORT=3306 DB_NAME=clawbot DB_USERNAME=root DB_PASSWORD=1234 \
REDIS_HOST=127.0.0.1 REDIS_PORT=6379 \
WX_TOKEN=clawbot-local-token ADMIN_INIT_PASSWORD=Admin@123456 \
java -jar target/clawbot-backend.jar
```

- 须显式设置 `SERVER_PORT`（如 8080），避免被运行环境注入的同名变量占用。
- 前端 `npm run build` 后由静态服务承载；开发态可跑 Vite dev server。

### 5.3 迁移与启动校验

1. Flyway 启动时自动执行 `db/migration`（V1.0.0 ~ **V1.0.14**），全 `success=1` 方可启动。
2. `GET /actuator/health` 返回 200、`status:UP`（db / redis / diskSpace 全 UP）。
3. 启动日志须出现 `ToolRegistry 初始化完成，已注册工具数=...`；该行为**工具契约校验通过的信号**——任一工具 Schema 非法或重名会在这一行之前直接终止启动（FR-23，§2.24）。
4. 改 `db/migration` 文件名后必须 `clean package`，避免残留陈旧脚本（Maven 资源增量复制不删除旧文件）。

### 5.4 Docker 编排（可选）

- `docker-compose.yml` 中 mysql 映射 `${DB_PORT:-3306}:3306`，与本机 MySQL84 冲突。
- 恢复二选一：先 `net stop MySQL84`；或改 `.env` 的 `DB_PORT`（如 13306）。
- Docker MySQL 初始口令由 `MYSQL_ROOT_PASSWORD` 决定（默认 1234），与本机库数据不互通。

---

## 第 6 章 维护指南

### 6.1 冷启动验证（结项必做）

- **教训**：单测全绿 ≠ 应用能启动。多构造器 Bean 未标 `@Autowired` 会在 `java -jar` 时失败。
- 结项前必须执行一次真实 `java -jar` 冷启动，确认 `Started ... in Xs` 且 `/actuator/health=200`。

### 6.2 已知启动级坑

| 现象 | 根因 | 处置 |
|---|---|---|
| 启动即 `NoSuchMethodException` | 多构造器 Bean 未标 `@Autowired` | 生产构造器加 `@Autowired` |
| Flyway 1064 语法错误 | 用了 MySQL 不支持的 `CREATE INDEX IF NOT EXISTS` | 改普通 `CREATE INDEX` |
| Flyway 拒绝继续 | `flyway_schema_history` 残留 `success=0` | 删除该行后以修正脚本重试 |

### 6.3 日志与诊断

- 限流 / 审计 / 工具调用均有落表，可用 SQL 检索。
- 避免直接 `tail` 含敏感串（token / 口令）的日志（触发安全审批拦截）；用 `grep -c` 只取计数。
- 多模型路由 `provider()` 可查真实生效模型。

### 6.4 端到端走查

```powershell
powershell -File scripts/e2e-smoke.ps1 -AdminPassword Admin@123456
```

- 脚本须 **UTF-8 带 BOM**（PS 5.1 兼容）；已弃用 `-SkipHttpErrorCheck`（PS7 专属），改用自写 `Invoke-WebRequestSafe`。
- AC-E3（OPERATOR 越权 403）需 `-OperatorToken` 补 token 重跑。

### 6.5 后端进程管理

- 进程存活判定以 `netstat :8080 LISTENING` 的 PID 为准，不能只看 `health=200`（可能命中遗留旧进程）。
- 清理遗留进程用 PowerShell `Stop-Process -Id <pid>`。

---

## 第 7 章 迭代交付、证据与规划

> 本章并入原 MVP / 迭代 2 / 迭代 3 的 PRD、架构、QA、验收与证据索引中的**关键结论与谱系**；过程细节不再单独保留（见 8.3）。迭代 4 规划见 [7.7](#77-迭代-4-规划前瞻)。

### 7.1 迭代 1（MVP，2026-09-17 ~ 09-18）

- **范围**：打通"微信（Mock）消息 → 验签/去重/路由 → 上下文 → 意图 → 工具编排 → 真实工具执行 → 结果回注 → 内容安全 + 执行一致性校验 → 落库 → 回复"端到端主链路 + 工程门禁。
- **目标**：G-1 主链路可离线跑通；G-2 "不可信的模型 + 可信的系统"（执行性幻觉泄漏 = 0）；G-3 每次"完成"可自证。
- **交付**：后端 `main` 源文件约 204 个；MVP 单测 163 项全过；前端 `vue-tsc + vite build` EXIT=0。
- **横向能力**：含 A-1 环境自检（Startup Doctor，`GET /api/doctor`）与 G3 占位符配置校验（`PlaceholderConfigValidator`），并在本手册 v1.2 补录（见 2.18）。
- **验收**：e2e `scripts/e2e-smoke.ps1` → **7 PASS / 0 FAIL / 1 SKIP**（AC-E3 越权因未提供 OPERATOR token 跳过）；`/actuator/health=200`；关键链路 `manage_pet_profile` 真实写入 `biz_pet_profile`（DB 直查确认）。
- **显式不在 MVP**：真实 LLM 供应商接入（用 Mock LLM 替代，预留 provider 切换位）、真实微信公众平台外发。

### 7.2 迭代 2（2026-09-18 ~ 09-19）

**M 波（T1–T9）**：补齐五类业务意图端到端能力（图片识别 FR-10 / 语音生成 FR-11 / 快递 FR-12 / 导航 FR-13 / 宠物档案 FR-14）+ 反幻觉基础版（FR-09 前移）+ 意图识别语料库 ≥300 条 + CI 门禁（JaCoCo / Checkstyle / SpotBugs）。验证：**223 测试全绿**。

**S 波（T10–T14）**：

| 任务 | 内容 | 落点 |
|---|---|---|
| T10 | FR-18 在线配置热生效 | `DynamicConfigService` + `ConfigAdminService` |
| T11 | FR-16 管理侧 | `UserStatusGate` + `UserAdminService` |
| T12 | A-2 回放调试台 | `ReplayService` + `Tool.readOnly()` |
| T13 | B-2 多模型路由 | `LlmClientRouter` |
| T14 | 前端 | 配置在线编辑 / 用户档案维护 / 回放台视图 |

验证：后端 `clean verify` → **281 测试 0 失败**（JaCoCo 达标、Checkstyle 0 违例、SpotBugs 0 bug）；前端 type-check / build / lint / test 通过；真实运行实测 6 项（配置热更新、非法值拒绝、审计留痕、回放护栏、拦截复现、用户启停留痕）。

### 7.3 迭代 3 Wave 1（2026-09-19）

| 任务 | 内容 | 落点 |
|---|---|---|
| T1 | FR-20 限流 | `RateLimitDecision` 枚举 + 用户/IP 令牌桶 + 白名单豁免 + Fail-Open；表 `log_rate_limit` |
| T2 | FR-19 数据保留 | 180 天清理 + 细粒度删除 + 匿名化 + 保留索引 |

验证：**310 测试全绿**；`java -jar` 冷启动 `Started in 11.9s`；Flyway 1.0.0–1.0.8 全 `success=1`；`/actuator/health=200`。

### 7.4 迭代 3 Wave 2（2026-09-20）

**范围**：迭代 3 收尾，交付 6 项（Wave 1 已交付 FR-20 限流 / FR-19 数据保留）。

| 任务 | 需求 | 提交 | 文件数 |
|---|---|---|---|
| T3 | FR-08 流式输出（SSE 观测通道） | `6d4128e` | 18 |
| T7 | A-4 档案变更留痕 | `4d30fd0` | 11 |
| T4+T5 | FR-17 监控 + A-3 降级看板 | `cad5e7a` | 25 |
| T6 | A-5 超时预算时序瀑布（V1.0.9） | `e2102a7` | 19 |
| T8 | FR-24 任务型多步会话 | `034172e` | 19 |

基线 `e9799fc`（Wave 1）；本轮新增 **5 个提交**。

**质量门禁（后端 `mvn clean verify`，本地直拉 Maven）**

```
Tests run: 355, Failures: 0, Errors: 0, Skipped: 0
JaCoCo: All coverage checks have been met.
Checkstyle: 0 violations.   SpotBugs: 0 bug.   BUILD SUCCESS
```

- 测试数演进：310（Wave 1）→ 316（T3）→ 322（T7）→ 330（T4+T5）→ 340（T6）→ **355（T8）**。
- 前端 `vue-tsc --noEmit` 与 `vite build` 均退出 0（监控视图含 echarts 独立分包）。

**真实冷启动验收（D8 纪律）**

| 项 | 结果 |
|---|---|
| `Started ClawbotApplication` | ✅ 出现 |
| `/actuator/health` | ✅ **200** |
| Flyway `flyway_schema_history` | ✅ 1.0.0 ~ **1.0.9 全部 `success=1`** |
| 新表 `log_orchestration_trace` | ✅ 已建（`log_audit` / `log_rate_limit` / `log_tool_call` 齐备） |
| `APPLICATION FAILED TO START` | ✅ 0 次 |
| 新增端点鉴权 | ✅ `/api/monitor/overview`、`/api/monitor/trace/{id}`、`/api/profiles/history`、`/api/tasks/{id}`、`/api/sse/console` 未授权均 **401** |

> 启动日志仅 1 条 Flyway WARN（MySQL「整数显示宽度将弃用」），非故障；冷启动完成后已停止服务并释放端口。

**逐需求验收映射**

| 需求 | 关键实现 | 证据（测试类） |
|---|---|---|
| FR-08 | `ConsoleController` + `ConsoleEventBuffer`（环形 100 / `Last-Event-ID` 续传 / 心跳）+ `ConsoleEventRelay` + 前端 `utils/sse.ts` | `ConsoleEventBufferTest` / `ConsoleEventRelayTest` / `AgentOrchestratorConsolePublishTest` |
| FR-17 | `MonitorService`（六类只读聚合）+ `MonitorController` + 前端三页四图 | `MonitorServiceTest`（8 例） |
| A-3 | 降级触发率 / 四层异常分布 / 幻觉拦截计数 / 泄漏 KPI | `MonitorServiceTest` + 编排器 `log_audit`（`reg_type=SAFETY`）埋点 |
| A-4 | `PetProfileServiceImpl.audit()` 字段级 diff（`normalize` 白名单 camelCase→snake_case，剔系统字段）+ `ProfileHistoryController` | `PetProfileAuditTest`(4) / `ProfileHistoryControllerTest`(2) |
| A-5 | 编排器 `System.nanoTime()` 埋点 → `OrchestrationTracedEvent` → `log_orchestration_trace` → 前端瀑布图 | `OrchestrationTraceEmitTest`(2) / `OrchestrationTraceServiceImplTest`(4) / `MonitorTraceControllerTest`(2) / `OrchestrationTraceListenerTest`(2) |
| FR-24 | `TaskSessionService`（槽位填充 / 2 上限栈 / 追问 ≤3 放弃 / 10min 超时 / 话题切换）+ Redis `task:{openid}` + `TaskController` | `TaskSessionServiceImplTest`(7) / `RedisTaskStoreTest`(4) / `TaskControllerTest`(3) / `TaskSessionOrchestrationIntegrationTest`(1) |

FR-24 四条验收准则逐一覆盖：① 缺单号 → 追问 → 仅回复单号 → 续接执行；② 连续 3 次无效 → 放弃（无第 4 问，BR-33）；③ 超时清理 Redis 键；④ 后台可见 `TASKING` 会话。

**口径局限（诚实性声明，均有事实依据）**

1. **四层异常分布仅覆盖 L3/L4**（详见 2.14）——L1/L2 未持久化，需补埋点。
2. **意图分布**以「工具调用归因」近似（详见 2.13）。
3. **FR-08 `message_delta`** 为终态一次性推送（详见 2.12）。
4. **FR-24 契约口径**：`taskType` 取触发工具名（非意图枚举）；槽位名取工具 Schema `required` 字段名，以保证 `resume` 可直接作工具入参；追问 / 放弃文案为确定性模板（不调 LLM，保证可测与时延可控）。

**过程记录（团队协作）**

- 采用 software-company SOP：架构师产出文件级分解与接口契约 → 工程师分任务实现 → 交付总监独立复验（重跑 `mvn verify` / 前端构建 / 真实冷启动，不采信自述）。
- `AgentOrchestratorImpl` 为 T3 / T5 / T6 / T8 的**收敛文件**，全程串行编辑；每个提交后独立复核生产构造器 `@Autowired`（防 D8）与提交范围（显式路径，隔离 `docs/` 遗留）。
- T7 首派 worker 因回报提示词超长失败（代码已落盘）；独立复验捕获并修复其 2 处缺陷（DELETE 分支漏 JSON 序列化致编译错误；字段名 camelCase 与前端 snake_case 契约不符），已重跑验证。

### 7.5 缺陷记录

| 编号 | 现象 | 根因 | 处置 |
|---|---|---|---|
| D3 | 工具调用序号 `call_seq` 恒 0 | 未以链路内序号入参 | 以链路内序号入参 `logStart` |
| D8 | `java -jar` 启动失败 `NoSuchMethodException: MediaTranscodeService.<init>()` | 多构造器 Bean 均未标 `@Autowired`，Spring 按无参优先查找失败 | 生产构造器加 `@Autowired` |
| D8′ | `CostBudgetServiceImpl` 同类问题 | 双构造器未标 `@Autowired` | 生产构造器加 `@Autowired` |
| D9 | `GET /api/compliance/report` 首调 500（NPE） | `tally` 只投影可空列，MyBatis 把「投影列全 NULL」的行映射成 null 元素；Mockito 桩恒为非 null 行，故 4 例全绿照不出 | null 行计入 `(空)` 桶 + 回归用例 `toleratesNullProjectedRows`（见 §7.10） |
| D10 | 前端**硬刷新/深链**任意受权限保护路由 → 白屏卡死（控制台报 Vue Router infinite redirection） | 令牌仅存 localStorage、角色不持久化；刷新后守卫在 `role=''` 状态判权限必失败 → 回退 `/dashboard` 又同样失败成环。SPA 内点菜单能正常（角色在内存），故历轮浏览器实证未触达 | 守卫改为：已认证但无角色时先 `await auth.fetchInfo()` 再判权限，取回失败回落登录页（随 W6-b 修复，见 §7.11） |
| — | 未匹配的 `/api/**` 路径返回 500 而非 404 | `NoResourceFoundException` 落进 `GlobalExceptionHandler` 的「未预期异常」分支 | 待处理（未纳入 W6 范围，仅记录） |
| — | V1.0.8 迁移 `CREATE INDEX IF NOT EXISTS` 报 1064 | MySQL 不支持该语法 | 改普通 `CREATE INDEX` |

> 固化教训：**单测全绿 ≠ 应用能启动**（D8）；**桩出来的形状 ≠ 真实映射的形状**（D9，只投影可空列时须按 null 元素设计）；新增/改造多构造器 Bean 必须显式标注装配入口；结项前须做一次 `java -jar` 冷启动验证（见 6.1）。

### 7.6 验证方式（证据类型）

| 类型 | 说明 |
|---|---|
| 单测 | JUnit5，`mvn -B -f backend/pom.xml clean verify`，报告在 `backend/target/surefire-reports/` |
| 独立验证 | `backend/src/test/.../verification/`（迭代 1 由 QA 独立编写 14 类 / 62 用例，不复用工程师测试类） |
| 集成 | `@Tag("integration")`，需 Docker/Testcontainers MySQL 8（本地未运行时不实跑） |
| 接口报文 | `scripts/e2e-smoke.ps1` 一条命令走查 |
| 冷启动 | 真实 `java -jar` + Flyway `success` 校验 + `/actuator/health`（结项必做，见 6.1） |
| 日志片段 | 结构化 JSON 日志（`traceId` + openid 脱敏） |

### 7.7 迭代 4 规划（前瞻）

> **状态声明**：本节为迭代 4 的**规划全景**；除明确标注「**已交付**」的条目（W4 → §7.8、W1 → §7.9、W6 → §7.10、W6-b → §7.11、W2 → §7.13、W3 → §7.14）外，其余**均未实现**，不得视为已完成或部分完成。SRS 4.5 的迭代规划表仍为唯一有效基线，下列修订**须经 SRS 11.3 变更流程确认后方可分配正式编号并生效**。


迭代 4 主题：**可演进 + 合规自证**，并在重新定位（[1.6](#16-重新定位说明演进记录)）方向上迈出"执行体"第一步。规划分两条轨道，**轨道 A 为 SRS 原生范围，轨道 B 为重新定位增量**。

#### 轨道 A：SRS 基线范围（4.5 + 12.8）

| 项 | 内容 | 交付判据 | 前置 |
|---|---|---|---|
| FR-22 | 配置热更新与**灰度开关**（含熔断回滚） | **已交付**（W2，见 §2.23 / §7.13）：熔断 60s 内回滚、行为可恢复（BR-31）两条判据有运行实证；灰度比例 ±2% 抽样以 1000 用户单测实证 | 已有 `DynamicConfigService`（迭代 2 落点） |
| FR-23 | 工具**插件化扩展接口** | **已交付**（W3，见 §2.24 / §7.14）：新增工具零改对话引擎（`QueryWeatherTool` 为活样本，引擎/Prompt 零 diff）、非法 Schema 与重名阻止启动有真实启动失败实证、契约以 `default` 方法保持向后兼容（BR-32） | 已有 `Tool` 契约 + `ToolRegistry` 启动期 Schema 校验 |
| FR-09 完整版 | 完整执行一致性校验（含反幻觉用例集自动化 TC-H 组） | 用例集自动化通过，误判案例纳入回归集 | 已有基础版（迭代 1/2） |
| B-3 | 轻量知识库 / RAG（结构性降低幻觉面） | 依 A-3 幻觉统计数据判断投入（E-07 经济性判据） | A-3 已交付（迭代 3）→ **统计已具备** |
| B-4 | 成本与配额治理看板 | 可视化 token 计量与配额消耗 | `LlmClient` token 计量落地 |
| B-5 | 合规报告自动化（一键导出留存 / 删除执行报告） | 报告可一键导出 | FR-19 已交付（迭代 3）→ **前置已满足** |

#### 轨道 B：重新定位增量（三层执行体，见 2.19 ~ 2.21）

| 层 | 目标 | 关键落点 | 依赖 |
|---|---|---|---|
| 个人状态库 | 从微信活动自动生长个人知识（人 / 地 / 物 / 承诺 / 偏好），带溯源 | **地基已交付（W6）**：`biz_memory_item` + 生长管道 + 溯源 + 召回注入（§2.19 / §7.10）；**后台只读界面已交付（W6-b，§7.11）**；承诺 → `biz_commitment` 留待 W8 | 无（可与轨道 A 并行） |
| 事件与触发引擎 | 订阅微信事件 / 定时 tick / 状态变化，决策"何时主动行动"；系统由 turn-driven 转 event-driven | 事件总线扩展 + 调度器 + 触发规则 | **须先确认服务号 / 企业微信主动触达能力**（订阅号无主动推送） |
| 主动执行器 | 模板消息 / 订阅通知 / 群@ / 定时推送；承诺闭环追踪 | 触达适配器 + 承诺状态机 + `remember`/`remind`/`followUp`/`recall`/`delegate` 工具 | 依赖事件与触发引擎 |

#### 建议任务分解（草案，待评审）

| 序 | 任务 | 轨道 | 说明 |
|---|---|---|---|
| W1 | L1/L2 异常埋点补全 | A（技术债） | **已交付**（379 测试全绿 + 冷启动实证；见 §7.9），补齐 2.14 四层分布缺口 |
| W2 | FR-22 灰度开关 + 熔断回滚 | A | **已交付**（457 测试全绿 + 干净库 `clawbot_w2` 冷启动，AC②③ / 异常流 2a 运行实证、AC① 抽样为单测实证；见 §7.13 / §2.23）；含回滚方案与灰度观测端点（观测台前端页 W2-b 待做） |
| W3 | FR-23 插件化契约与启动校验 | A | **已交付**（485 测试全绿 + 干净库 `clawbot_w3` 冷启动，AC② 非法 Schema 真实启动失败、AC③ 日志/`log_tool_call`/看板归因域运行实证、AC① 以 diff 为证；见 §7.14 / §2.24）。工具分组授权（备选流 2a）与工具级限流（4a）**未做** |
| W4 | B-5 合规报告导出 | A（低成本） | **已交付**（359 测试全绿；前端入口 `/compliance/export` 亦已补齐，见 §2.22 / §7.12） |
| W5 | B-4 成本看板 | A | 依赖 token 计量 |
| W6 | 个人状态库（表 + 生长管道 + 溯源） | B | **已交付**（417 测试全绿 + 干净库冷启动与生长/删除实证；见 §7.10），三层能力的地基 |
| W6-b | 状态库只读后台（端点 + 前端页） | B | **已交付**（424 测试全绿 + 干净库冷启动 + 浏览器实证；见 §7.11），闭合 §7.10「无后台界面」缺口 |
| W7 | 事件与触发引擎（调度 + 规则） | B | 架构心脏 |
| W8 | 主动执行器 + 承诺闭环 | B | 依赖 W7 |
| W9 | FR-08 真 token 流式 | A（技术债） | `LlmClient.stream()` 重构（补齐 2.12 局限） |

#### 前置条件与待确认项（阻塞项，须先解决）

| 编号 | 待确认 | 影响 |
|---|---|---|
| OI-05 | 多模型 A/B 与灰度发布的具体策略 | **已解除**（2026-09-20 决策 + W2 交付落地，见 §2.23 / §7.13）；FR-22 三条验收判据已有运行实证 |
| 「主动触达」通道 | 服务号 / 企业微信的模板消息与订阅通知可用性 | 阻塞轨道 B 全部（无通道则主动执行无法落地） |
| 候选约束 G1~G4 | BR-35（"完成"须附可复现证据）、NFR-MA-10/11、NFR-SE-13（占位符配置 Fail-Fast） | 未确认前不得在测试用例中引用其编号 |

> **决策记录（2026-09-20，用户拍板）**：① OI-05 → 采用「白名单命中 + 比例灰度(1%→5%→20%)」双轨，须明确回滚触发条件（错误率/时延阈值），满足 BR-31；W2 已据此交付（白名单 + 稳定分桶比例 + 错误率/时延双阈值熔断回滚，见 §2.23 / §7.13）。② 主动触达通道 → 暂不确定，需评估；W7/W8 暂缓，W6 个人状态库不依赖通道可并行。③ G1~G4 → 走 SRS 11.3 变更流程确认后生效，确认前测试不引用其编号。④ **首发任务定为 W4 B-5 合规报告导出**（前置 FR-19 已满足、低成本、纯内部、无外部依赖），**已交付**（359 测试全绿并推送 main；详见 §7.8 / §2.22）。⑤ **W3 的插件化口径（2026-09-21 决定）**：派生口径一律「工具自述 + 注册表反查」，不在应用层留工具名清单；默认值取**安全侧**（词表空集 / 归因 `chat` / 意图 `null`）；示例工具 `query_weather` 以 FR-18 开关**暗启动**（默认禁用），使"新增工具"这件事对既有行为零影响，同时留下可放量取证的真样本（详见 §7.14）。

#### 明确不做（迭代 4）

- 不引入微服务化、不拆分共享库（坚持 ADR-001 分层单体 + ADR-011 独立项目）。
- 不做人工灌库式知识库（个人状态库必须"生长式"，见 2.19）。
- 不对外提供工具市场 / 插件生态（工具为内部执行能力）。

---

### 7.8 迭代 4 首发任务（W4 B-5 合规报告）交付与证据

- **范围**：B-5 合规报告自动化（一键导出留存/删除执行报告），设计详见 [§2.22](#222-合规报告自动化b-5--w4已实现)。
- **落点文件**（6 主源 + 1 测试）：
  - `backend/src/main/java/com/lumensteward/clawbot/application/compliance/`（聚合 record `ComplianceReport` + 端口 `ComplianceReportService`）
  - `backend/src/main/java/com/lumensteward/clawbot/infrastructure/compliance/ComplianceReportServiceImpl.java`
  - `backend/src/main/java/com/lumensteward/clawbot/interfaces/admin/ComplianceController.java`
  - `backend/src/main/java/com/lumensteward/clawbot/interfaces/assembler/ComplianceReportAssembler.java`
  - `backend/src/main/java/com/lumensteward/clawbot/interfaces/dto/compliance/ComplianceReportVO.java`
  - `backend/src/test/java/com/lumensteward/clawbot/application/compliance/ComplianceReportServiceImplTest.java`（4 例）
- **无新增 Flyway 迁移**：报告只读聚合既有表，未引入新表（符合 B-5 「一键导出」定位，亦规避迁移回归风险）。
- **验证（本轮实跑，非推断）**：
  - 全量后端门禁：`mvn -B clean verify` → **`Tests run: 359, Failures: 0, Errors: 0, Skipped: 0`** + `0 Checkstyle violations` + SpotBugs 通过 + `BUILD SUCCESS`（迭代 3 基线 355 + 本模块 4 例）。
  - 单测：`ComplianceReportServiceImplTest` 4 例（基数/保留天数、删除按范围 ALL/CHAT/PET 聚合 + `anon_` 匿名化计数、工具调用失败率、限流/编排分节 + Markdown 脱敏断言），基于 Mockito 桩 8 个 Mapper；因未启动 MyBatis 上下文，`@BeforeAll` 须以 `TableInfoHelper.initTableInfo` 注册被 Lambda 条件引用的 5 个实体（与既有 `MyMetaObjectHandlerTest` 同一套路）。
- **提交前修复清单（原代码不可编译 / 测试不可运行，均已实证修复）**：
  1. `ComplianceReportAssembler:60` 字符字面量 `'\n\n'` → 字符串 `"\n\n"`（**主源码编译错误**，javac 报「未结束的字符文字」）。
  2. 同文件 `appendRetention` 删除无用的 `ComplianceReportVO retention = vo;` 死变量（SpotBugs 风险项）。
  3. 测试类补 `ComplianceReportServiceImpl` 跨包 import；补 `@BeforeEach` 空集桩（Mock 默认返回 null 致 `selectPage` NPE）；`DATA_DELETE` 计数桩与断言对齐。
  4. `ComplianceReport` 中 `failureRate` / `blockRate` 的 Javadoc 口径由「0~1」更正为实际实现「百分比 0~100」。
- **更正说明（诚实性）**：本节先前记「e2e 8/8 走查证明含合规代码可编译」不成立——该 e2e 运行的是不含本模块的旧 jar，本模块此前从未被编译过。现以全量 `clean verify` 359 绿为唯一编译与回归证据。
- **状态**：已提交（见 §9.1）。

---

### 7.9 迭代 4 W1（L1/L2 异常埋点补全）交付与证据

- **范围**：把 SRS 2.3.5 中此前**不可见**的 L1 接入层 / L2 认知层异常落成可查事实，补齐 §2.14 四层分布的恒 0 缺口。设计取舍见该节「四层分布的数据来源」。
- **落点文件**（7 主源 + 1 迁移 + 5 测试）：
  - `backend/src/main/resources/db/migration/V1.0.10__anomaly_event.sql` —— 新表 `log_anomaly_event`（只增不改，`layer` 显式列，`openid` 存脱敏形态，两条独立 `CREATE INDEX`）
  - `common/enums/AnomalyLayer.java` —— L1~L4 层次枚举
  - `application/anomaly/AnomalyNotice.java` + `AnomalyNoticeListener.java` —— 事件与订阅（编排器经 `ApplicationEventPublisher` 解耦，不新增构造器依赖）
  - `infrastructure/persistence/entity/AnomalyEventEntity.java` / `mapper/AnomalyEventMapper.java` / `service/AnomalyEventService(Impl).java` —— 落库侧集中脱敏 + `detail` 截断 255 + best-effort
  - 埋点改造：`interfaces/wechat/WechatCallbackController.java`（验签失败 / 时间戳越界 / 幂等重复）、`application/dispatcher/MessageDispatcher.java`（未知类型回落）、`application/orchestrator/AgentOrchestratorImpl.java`（两处 `catch (LlmException)`）
  - 看板聚合：`application/admin/MonitorService.java`（`anomalyDistribution` 改为两表合并）
  - 测试：`AnomalyEventServiceImplTest`(6) / `AnomalyNoticeListenerTest`(2) / `UnknownMsgTypeAnomalyTest`(3) / `WechatCallbackAnomalyTest`(5) / `OrchestratorAnomalyEmitTest`(3) + `MonitorServiceTest` 新增 1 例合并断言
- **兼容性处理**：`WechatCallbackController` / `MessageDispatcher` / `MonitorService` 构造器各加一参（**不写向后兼容壳构造器**），相应 6 处既有测试与 verification 调用点同步补 `null`。
- **验证（本轮实跑，非推断）**：
  - 全量后端门禁：`mvn -B -o clean verify` → **`Tests run: 379, Failures: 0, Errors: 0, Skipped: 0`**（W4 基线 359 + 本任务 20 例）+ **`0 Checkstyle violations`** + **SpotBugs `BugInstance size is 0`** + `BUILD SUCCESS`。
  - **冷启动（D8 纪律）**：真实 `java -jar target/clawbot-backend.jar --server.port=8081` → `Successfully applied 1 migration`、`Started ClawbotApplication in 9.997s`、`GET /actuator/health = 200`；`flyway_schema_history` 中 `version=1.0.10 / success=1`，`describe log_anomaly_event` 列与迁移一致。
  - **运行期实证**：对真实启动的实例执行 `scripts/e2e-smoke.ps1`，其中 AC-A2 的 100 次伪造签名与 AC-A3 的同 MsgId 10 次重投，在 `log_anomaly_event` 产出 **L1 `SIGNATURE_INVALID`** 与 **L1 `MSG_DUPLICATED` 9 条（= 10−1，与幂等判据自洽）**，且每行都带 `trace_id`；等价 SQL `select layer, count(*) from log_anomaly_event where created_at >= 当日零点 group by layer` 返回 `L1 = 227`（改动前该值恒为 0）。
  - 本轮 e2e 结果：**AC-A4 / AC-A1 / AC-A2 / AC-A3 四项微信接入层判据 PASS**；`AC-E1`（登录取 JWT）FAIL、`AC-E3` / `AC-C1` SKIP——本机 `clawbot` 库中 `superadmin` 口令由更早会话注入、本轮未知且未重置，属**环境凭据问题**，与本任务改动无关（同一实例上接入层四项全绿可佐证服务本身正常）。
- **未取得证据（如实标注，均已由 §7.10 的干净库冷启动补齐）**：
  - **L2 埋点无运行期样本** → 已由 §7.10「顺带补齐的 W1 遗留证据」闭合（`L2 / LLM_UNAVAILABLE` 落表 + 看板四层一致）。
  - **A-3 看板 HTTP 层未复跑** → 同上闭合（`GET /api/monitor/degrade` 与等价 SQL 一致，六个看板端点均 200）；合并逻辑另有 `MonitorServiceTest` 新例（L1=4 / L2=2 / L3=3 / L4=0，非 L1~L4 的脏行被忽略）。
  - **留存清理未覆盖**：`log_anomaly_event` 未纳入 180 天清理（现状与 `log_audit` / `log_rate_limit` / `log_orchestration_trace` 一致）；表内 `openid` 为脱敏形态，残余风险有限，但长期增长需在留存任务中统一处理。
- **测试期望的一次自我更正**：曾断言「空白 msgType 视为未知类型并埋点」，实跑失败——`normalize()` 把空白归一为默认 `text` 并命中已注册处理器，属正常回落而非非法类型。已按实际语义改为「不埋点」，SRS 2.3.5 的 L1 判据仅对**未注册类型**成立。
- **状态**：已提交（见 §9.1）。

---

### 7.10 迭代 4 W6（个人状态库：表 + 生长管道 + 溯源 + 召回）交付与证据

- **范围**：轨道 B 地基（§7.7 W6）——跨会话生长的个人知识落库、自动抽取、带溯源、注入召回，并接入 PII 治理三处。能力设计见 [§2.19](#219-个人状态库w6地基已落地)。
- **落点文件**（14 主源 + 2 迁移 + 6 新测试）：
  - 迁移：`V1.0.11__memory_item.sql`（`biz_memory_item`，含 `live_marker` 生成列上的 `(openid,kind,name)` 唯一索引）、`V1.0.12__seed_memory_config.sql`（5 个 `memory.*` 配置键播种）
  - 领域：`common/enums/Memory{Kind,Status,Origin}.java`、`domain/memory/{MemoryWrite,MemoryFact,MemoryStore}.java`
  - 应用：`application/memory/{MemoryGrowthNotice,MemoryExtractor,LlmMemoryExtractor,MemoryGrowthListener,MemoryRecallService}.java`、`application/config/ConfigKeys.java`（5 个键）
  - 基础设施：`infrastructure/persistence/{entity/MemoryItemEntity,mapper/MemoryItemMapper,repository/MemoryStoreImpl}.java`
  - 接线：`application/orchestrator/AgentOrchestratorImpl.java`（召回注入 + 生长发布，构造器第 17 参）、`mock/llm-scripts.yml`（`memory-extract` 规则置于首位）
  - PII 治理：`infrastructure/retention/{DataRetentionServiceImpl,UserDataDeletionServiceImpl}.java`、`application/retention/{DataRetentionService,UserDataDeletionService}.java`、`application/compliance/ComplianceReport.java` + `infrastructure/compliance/ComplianceReportServiceImpl.java` + `interfaces/assembler/ComplianceReportAssembler.java`
  - 测试：`MemoryStoreImplTest`(9) / `LlmMemoryExtractorTest`(10) / `MemoryGrowthListenerTest`(4) / `MemoryRecallServiceTest`(6) / `MemoryGrowthMockScriptTest`(3，加载**真实** `mock/llm-scripts.yml`) / `OrchestratorMemoryWiringTest`(4)
- **验证（本轮实跑，非推断）**：
  - 全量后端门禁：`mvn -B -o clean verify` → **`Tests run: 417, Failures: 0, Errors: 0, Skipped: 0`**（W1 基线 379 + 本任务 37 + 下方缺陷修复 1）+ **`0 Checkstyle violations`** + **SpotBugs `BugInstance size is 0`** + `BUILD SUCCESS`。
  - **干净库冷启动（D8 纪律）**：新建空库 `clawbot_w6` → 真实 `java -jar target/clawbot-backend.jar --server.port=8081`（带 `WX_TOKEN` / `ADMIN_INIT_PASSWORD`）→ `Successfully applied 13 migrations ... now at version v1.0.12`、`Started ClawbotApplication in 9.309 seconds`、`GET /actuator/health = 200`；`describe biz_memory_item` 17 列与迁移逐列一致，`memory.*` 5 行按预期值播种（growth=false / recall=true）。
  - **e2e 走查 8 PASS / 0 FAIL / 0 SKIP**（同一实例）：含 **AC-E1 登录取 JWT**、**AC-E3 越权 403**、**AC-C1 对话建档写库**——这三项此前因本机 `clawbot` 库 superadmin 口令未知而**未取得证据**，本轮以「干净库 + `ADMIN_INIT_PASSWORD` 播种」绕过凭据障碍（未重置、未猜测既有库口令）。
  - **生长运行期实证**：经 `PUT /api/configs` 把 `memory.growth.enabled` 热改为 `true`（免重启），随后 `scripts/e2e-smoke.ps1` 的 3 个 openid 各自产出 2 条条目：`origin=AUTO_EXTRACT`、`extractor=llm-extract-v1`、`confidence=0.800/0.700`、`source_session_id` 与 `source_trace_id` 均非空（共 6 行、3 个 openid，互不串味 = BR-07）；监听器日志形如 `状态库生长完成 openid=e2e_****d_a1 候选=2 新增=2 覆盖=0 强化=0 入队丢弃=0`（openid 已脱敏）。
  - **强化语义实证**：向同一 openid 再发一条消息 → 仍只有 2 行，`hit_count` 由 1 变 2（重复提及强化而非复制）。中文内容完整性经 `CHAR_LENGTH`/`LENGTH` 核对（10 字符 / 30 字节），确认控制台乱码只是 Windows 终端编码、库内为正确 `utf8mb4`。
  - **删除可证明实证（FR-19 ② / BR-27）**：`DELETE /api/users/2/data?scope=chat` 返回 `{"scope":"CHAT","messages":2,"sessions":1,"pets":0,"memories":2,...}`，库内该 openid 条目归零、**另外两个 openid 的条目不受影响**。
  - **合规报告**：`GET /api/compliance/report` 返回 200，含「状态库已覆盖历史保留天数：180 天」与 `biz_memory_item` 基数行；全文 `e2e_openid` 计数为 0（BR-21）。
  - **降级不生长实证**：以 `--llm.base-url=http://127.0.0.1:9/v1` 令 LLM 不可达后打一次回调，该轮走兜底，`biz_memory_item` 行数不变（4 → 4）且监听器零日志。
- **顺带补齐的 W1 遗留证据（同一实例，未改动 W1 代码）**：LLM 不可达那一轮在 `log_anomaly_event` 落出 **`L2 / LLM_UNAVAILABLE / openid=e2e_****d_a1 / detail=round=0`，`trace_id` 非空**；`GET /api/monitor/degrade` 的 `anomalyDistribution` 返回 `L1=109 / L2=1 / L3=0 / L4=0`，与等价 SQL `select layer, count(*) from log_anomaly_event group by layer` 完全一致（AC-E5）。至此 §7.9 的「L2 运行期样本」与「看板 HTTP 层」两项已闭合。
- **修复的一处既有缺陷（W4 合规报告，首次真实调用才暴露）**：`GET /api/compliance/report` 冷启动首调返回 **500**，堆栈定位 `ComplianceReportServiceImpl.tally`。根因：`tally` 只投影可空列（`log_tool_call.error_type`），MyBatis 对「投影列全为 NULL」的行会映射出 **null 元素**（成功的工具调用即此形态），`keyExtractor.apply(null)` 抛 NPE。Mockito 桩里的行对象恒非 null，故单测全绿也照不出来——与 D7/D8 同一类教训（**桩出来的形状不等于真实映射的形状**）。修复：`tally` 把 null 行计入 `(空)` 桶，并新增回归用例 `toleratesNullProjectedRows`（以 `Arrays.asList(null, entity)` 复现真实映射）。修复后该端点 200 且 `按异常分类(error_type)：(空)=1` 正确呈现。
- **未取得证据 / 已知缺口（如实标注）**：
  - **召回块的运行期证据缺失**：注入的 system 块不落任何可查表，Mock LLM 也不回显请求体，故「召回真的进了本轮上下文」目前只有 `OrchestratorMemoryWiringTest`(4) 的单元级证据；需在真实 provider 下以请求日志或回答内容复核。
  - **SUPERSEDED 链与留存清理无运行期样本**：Mock 抽取脚本产出恒定，造不出「同名不同值」；覆盖路径与 `purgeSupersededMemories` 仅有单元证据（`MemoryStoreImplTest` 的覆盖顺序 / 溯源继承、`DataRetentionServiceImplTest` 的 null 截止不动库）。留存任务 `cron 0 0 3 * * ?` 本轮未等待触发。
  - **真实模型成本未测**：生长每轮多一次抽取调用，token 已计入 `CostBudgetService`，但实际增量倍率取决于真实 provider，本轮仅 Mock 路径。
  - **无后台界面（W6-b）** → 已由 §7.11 闭合（`GET /api/memories` / `DELETE /api/memories/{id}` + 前端只读页 + 浏览器实证）。
- **兼容性处理**：`AgentOrchestratorImpl` 主构造器增至 17 参（15 参壳保留并委托）；`DataRetentionServiceImpl` / `UserDataDeletionServiceImpl` / `ComplianceReportServiceImpl` 各加一参且**不写壳构造器**，4 处既有测试同步补桩。
- **状态**：已提交（见 §9.1）。

---

### 7.11 迭代 4 W6-b（状态库后台：只读列表 + 纠错删除）交付与证据

- **范围**：轨道 B 收尾缺口（§7.7 W6-b）——状态库运营侧只读界面与人工纠错删除（§2.19「后台界面」）。无新迁移、无表变更、不触碰 W6 写入链路。
- **落点文件**：
  - 后端（4 主源 + 2 测试）：`interfaces/dto/memory/MemoryItemVO`、`application/admin/MemoryAdminService`（分页查询 + 逻辑删除 + `log_audit(reg_type=MEMORY)` 留痕）、`interfaces/admin/MemoryController`、`MaskingAssembler.toMemoryItemVO`（新增装配方法）；测试 `MemoryAdminServiceTest`(4) + `MemoryControllerTest`(3，用**真实** MaskingAssembler 验脱敏)。
  - 前端（7 处）：`config/permissions.ts`（`memory:view` 三角色 / `memory:delete` 仅 SUPER_ADMIN）、`router/index.ts`、`layouts/MainLayout.vue`、`services/memory.api.ts`、`types/memory.ts`、`views/memory/index.vue`、`utils/constants.ts`（`MEMORY_{KIND,STATUS,ORIGIN}` 中文映射）。
- **验证（本轮实跑）**：
  - 全量后端门禁：`mvn -B -o clean verify` → **`Tests run: 424, Failures: 0, Errors: 0, Skipped: 0`**（W6 基线 417 + 本任务 7）+ **`0 Checkstyle violations`** + **`BugInstance size is 0`** + `BUILD SUCCESS`。
  - 前端：`vue-tsc --noEmit` 通过；`vite build` 成功；本次改动文件 `eslint --max-warnings 0` 干净（既有 `views/monitor/console.vue` 有历史格式告警，非本次引入，未顺手处理）。
  - 干净库冷启动：新库 `clawbot_w6b` + `java -jar`（8081，带 `WX_TOKEN` / `ADMIN_INIT_PASSWORD`）→ 13 迁移至 v1.0.12、health 200；`e2e-smoke.ps1` **8 PASS / 0 FAIL**（含 AC-E1 登录、AC-E3 越权 403、AC-C1 对话建档），并以同一初始口令取得 OPERATOR 令牌。
  - **HTTP 实证**（开 `memory.growth.enabled=true` 生长出 6 条后）：SUPER_ADMIN `GET /api/memories` 200，`total=6`，出参 openid 形如 `e2e_****d_a3`（BR-21），置信度/提及数/`sourceTraceId` 齐全；过滤 `kind=PERSON&openid=…a3` 命中 1 行、`status=SUPERSEDED` 0 行、分页 `pageSize=3` 生效。OPERATOR 读 200、删 **403**；SUPER_ADMIN 删 200；重复删同一 id 返回 **30005「状态库条目不存在」**；`log_audit` 出现 `MEMORY/DELETE` 行（`admin_id=1`、target 脱敏、快照仅 kind/name/content/origin）。
  - **逻辑删除语义实证**：删除后 `deleted_at` 置位、列表 total 6→5 且该行消失；同 openid 再发一条消息 → 被删的 `(openid, PERSON, 名称)` 槽位**重新生长**为新行（未被逻辑删除的 PREFERENCE 行则原地强化 `hit_count` 1→2），与 `live_marker` 生成列设计一致。
  - **浏览器实证**（Vite 5173 代理至 8081）：SUPER_ADMIN 侧边栏出现「个人状态库」，深链硬加载列表 6 行渲染正常，点删除 → toast「已删除条目 #2（逻辑删除，审计留痕）」、行消失、库内新增第二条 MEMORY 审计；OPERATOR 硬加载同页可读、删除按钮 0 个（`v-permission` 移除）；既有深链 `/sessions`、`/configs` 回归正常。
- **顺带修复的既有缺陷（D10，§7.5）**：前端**任意受权限保护路由的硬刷新/深链**白屏成环——令牌持久化而角色不持久化，守卫在 `role=''` 判权限必失败 → 回退 `/dashboard` 又同样失败。A/B 实证为既有缺陷：暂存本轮全部改动后基线代码硬加载 `/sessions` 同样成环。修复：守卫在已认证但无角色时先 `await auth.fetchInfo()` 再判权限，失败回落登录页。
- **未取得证据 / 缺口（如实标注）**：
  - **AUDITOR 运行期样本**：本机仅播种 superadmin / operator 两账号，AUDITOR 可读列表口径仅有 `@PreAuthorize` 声明与权限表为证。
  - 守卫修复（D10）无前端单测（项目 vitest 无既有用例），证据为浏览器行为。
  - 纠错删除不带 reason（`log_audit.reason` 为 NULL）；§2.19 剩余缺口见该节。
- **状态**：已提交（见 §9.1）。

---

### 7.12 迭代 4 W4 前端入口（合规报告导出页）交付与证据

- **范围**：补 §2.22 遗留的「后端端点已就绪、前端无入口」缺口。纯前端任务：**后端零改动、无新迁移、无新端点**（`GET /api/compliance/report` 原样复用）。
- **落点文件**（前端 5 处）：`services/compliance.api.ts`（原生 fetch 取 blob + 解析 `Content-Disposition` 文件名，本地按同规则兜底）、`views/compliance/export.vue`（六大主题说明 + 导出按钮 + 本会话最近一次导出）、`config/permissions.ts`（`compliance:export`，仅 SUPER_ADMIN）、`router/index.ts`（`/compliance/export`）、`layouts/MainLayout.vue`（菜单「合规报告导出」）。
- **验证（本轮实跑）**：
  - 后端门禁（确认零回归）：`mvn -B -o clean verify` → **`Tests run: 424, Failures: 0, Errors: 0, Skipped: 0`** + `BUILD SUCCESS`（与 W6-b 同基线，本轮未增删测试）。
  - 前端：`vue-tsc --noEmit` 通过；`vite build` 成功；本次 5 文件 `eslint --max-warnings 0` 干净；`vitest run` 无既有用例（与 §7.11 同一现状）。
  - 干净库冷启动：复用 `clawbot_w6b` + `java -jar`（8081，带 `WX_TOKEN` / `ADMIN_INIT_PASSWORD`）→ health 200。
  - **HTTP 实证**：SUPER_ADMIN `GET /api/compliance/report` → **200**、`Content-Type: text/markdown;charset=UTF-8`、`Content-Disposition: attachment; filename=compliance-report-20260921.md`、正文 2822 字节含 7 个 `## ` 分节（六大主题 + 声明）、`biz_memory_item` 基数行在列、原文 `e2e_openid` 计数 **0**（BR-21）；OPERATOR 同请求 → **403 / code 20003 权限不足**。
  - **浏览器实证**（Vite 5173 代理至 8081，页面须以 `http://localhost:5173` 打开：`clawbot.cors.allowed-origins` 仅放行 localhost 源，用 `127.0.0.1` 会被判 `Invalid CORS request`）：SUPER_ADMIN 侧边栏出现「合规报告导出」，点菜单进页 → 点导出 → 网络面板 `GET /api/compliance/report [200] fetch mimeType=text/markdown`，页面显示「本次会话最近一次：compliance-report-20260921.md」、toast「已导出 …」，且文件**真实落盘** `C:\Users\ASUS\Downloads\compliance-report-20260921.md`（复核其内容为 7 分节、`e2e_openid` 0 次、含「状态库已覆盖历史保留天数：180 天」）；控制台除 Vite 连接调试信息外无报错。OPERATOR 侧边栏无该项菜单，深链硬加载 `/compliance/export` 由守卫回退 `/dashboard`（顺带复验 §7.11 D10 修复对新路由同样生效）。
- **未取得证据 / 缺口（如实标注）**：
  - **AUDITOR 运行期样本**依旧缺失（本机只播种 superadmin / operator）；其不可见仅有权限表（`ROLE_PERMISSIONS.AUDITOR` 无 `compliance:export`）与后端 `@PreAuthorize` 声明为证。
  - 前端无单测覆盖（项目 vitest 无用例基线），证据为浏览器行为。
  - 报告正文本身的分节口径与计算逻辑由 W4 既有单测与 §7.8 覆盖，本轮未改动。
- **状态**：已提交（见 §9.1）。

### 7.13 迭代 4 W2（FR-22 灰度开关 + 熔断回滚）交付与证据

- **范围**：轨道 A 的 FR-22（配置热更新 + 灰度开关 + 熔断回滚），设计取舍见 [§2.23](#223-灰度开关与熔断回滚fr-22--w2已实现)。闭合 OI-05 决策（§7.7）与 BR-31「无回滚方案不得上线」的落地出口。**无新表**：只读 `log_orchestration_trace` / `log_anomaly_event`，写入 `sys_config`（V1.0.13 播种 7 键）与 `log_audit`。
- **落点文件**（12 主源 + 1 迁移 + 5 新测试 + 3 处既有用例扩充）：
  - `application/gray/`：`GrayFeature`（受治理功能枚举，代号即哈希盐值）、`GrayReleaseService`（判定顺序 + SHA-256 稳定分桶）、`GrayHealthMetrics`（端口）/ `GrayHealthSnapshot`、`GrayCircuitBreaker`（30s 定时评估）、`GrayRollbackService`（一键回滚）
  - `infrastructure/gray/GrayHealthMetricsImpl`（读既有两表求轮次/异常/P95/预算超限，**分子只取 L2~L4**）
  - `interfaces/admin/GrayController` + `interfaces/dto/gray/{GrayStatusVO,GrayPreviewVO,GrayRollbackRequest,GrayRollbackVO}` + `MaskingAssembler.toGrayPreviewVO`
  - 接线与校验：`application/config/ConfigKeys`（7 个 `gray.*` 键）、`application/admin/ConfigAdminService`（`gray.*.percent` 值域 0~100 校验）、`application/memory/MemoryGrowthListener`（主开关 && 灰度命中）
  - 迁移：`V1.0.13__seed_gray_config.sql`（7 行 `category=gray`，比例默认 **100**）
  - 测试：`gray/GrayReleaseServiceTest`(9) / `gray/GrayCircuitBreakerTest`(7) / `gray/GrayRollbackServiceTest`(4) / `interfaces/admin/GrayControllerTest`(5) / `infrastructure/gray/GrayHealthMetricsImplTest`(5)；扩充 `admin/ConfigAdminServiceTest`(+2 灰度值域)、`application/memory/MemoryGrowthListenerTest`(+1 灰度回滚即停生长)、`verification/RbacSurfaceVerificationTest`（控制器面补 `MemoryController` / `GrayController`）
- **验证（本轮实跑，非推断）**：
  - 全量后端门禁：`mvn -B -o clean verify` → **`Tests run: 457, Failures: 0, Errors: 0, Skipped: 0`**（W6-b 基线 424 + 本任务 30 + 扩充 3）+ **`0 Checkstyle violations`** + **SpotBugs `BugInstance size is 0`** + `BUILD SUCCESS`（告警分级修复后重跑仍为 457 全绿）。
  - **AC①（灰度比例与抽样）**：`GrayReleaseServiceTest` 以 1000 个合成用户按 `percent=10` 分桶，命中数落在 **80~120**（±2% 内），且 1%→5%→20% 逐级放量时命中集合**单调扩张**（不洗牌）——这是 SHA-256 分桶相对 `String#hashCode()` 的取舍依据。
  - **干净库冷启动（D8 纪律）**：新库 `clawbot_w2` + 真实 `java -jar`（8081，带 `WX_TOKEN` / `ADMIN_INIT_PASSWORD`）→ Flyway **14 个迁移执行至 v1.0.13**、`Started ClawbotApplication in 11.075s`、`GET /actuator/health = 200`（修复后二次冷启动 8.902s / 200）。
  - **运行期分流实证**：`percent=20` 时三个测试用户的桶号为 **15 / 96 / 38**（15 命中、96/38 未命中），且**回滚 → 重新放量后桶号完全不变**；白名单 `gray_u1,gray_u9` 在比例 > 0 时以 `WHITELIST` 命中；`percent=0` 时同一批用户一律 `GRAY_OFF`（**白名单一并失效 = 一刀切**）。
  - **AC②（60s 内自动回滚）**：第一轮在健康度数据入窗后 **22s** 完成回滚（`log_audit` #14/#15，14:30:31）；告警分级修复后的复测轮，实例调度启动后**首个评估 tick（31s）**即完成回滚——`gray.memory_growth.percent` 100→0，逐项 `CONFIG_UPDATE`（`admin_id` **NULL** = 系统触发）+ 一行 `reg_type=GRAY / action=ROLLBACK`（`target=gray.*.percent`、`before_value=memory_growth=100`、`after_value=percent=0`、reason 含实测错误率与窗口样本）。判据来自近窗 20 轮 / 25 条 L2 异常 = 错误率 **125% > 30%**。
  - **AC③（行为恢复到变更前）**：回滚后同一批灰度用户再发消息，`biz_memory_item` **无新增行**；此前在灰度内的用户只出现 `hit_count` 1→2 的原地强化，回滚后连强化也停止——与"灰度前行为"完全一致。
  - **异常流 2a（比例越界）**：`PUT /api/configs` 置 `gray.memory_growth.percent=101` 或 `-1` → **400 / code 10002**（`灰度比例必须在 0~100 之间`），库值不变；未播种的 `gray.*` 键 → 「配置项不存在」拒绝。
  - **RBAC 与脱敏（BR-21 / BR-23 / BR-25）**：OPERATOR `GET /api/gray/status` 与 `/preview` 均 **200**、`POST /rollback` **403**；SUPER_ADMIN 回滚 200。`status` 只透出 `whitelistSize` **计数**而非名单，`preview` 出参 openid 形如 `****` / `wx-o****d-a1`，响应体中原始 openid 计数 **0**；回滚请求 `reason` 空白 → **400 / 10001**。
  - **幂等与可观测**：无在放量功能时再按回滚 → `rolledBack=0`、**不写审计行**；`GET /api/gray/status` 同屏给出阈值（`errorRatePercent=30 / minSamples=20 / windowMinutes=5 / p95Ms=0 / checkIntervalSecond=30`）与实测健康度（`turns=20 / anomalies=25 / errorRatePercent=125.00 / p95LatencyMs=1200`），使"为什么回滚"可被运维当场复核。
- **本轮修掉的运行期缺陷（告警疲劳）**：首版熔断在真实回滚后，只要越界窗口未过就**每 30s 重复打一条 ERROR**（修复前实测一轮内 **10 条 ERROR / 0 条 WARN**），会淹没真告警。已改为：仅在 `rolledBack > 0` 时打 ERROR（"已自动回滚 N 项，请核查"），无可回滚项时降为 WARN 并说明不重复告警。修复后实证：**ERROR 恰 1 条（15:12:19，真实回滚那次）+ WARN 3 条**（15:12:49 / 15:13:19 / 15:13:49）。
- **数据卫生**：验证注入的 20 条 `log_orchestration_trace` 与 25 条 `log_anomaly_event` 种子行在取证后**已全部删除**（临时库 `clawbot_w2` 非仓库内容，可随时删除重建）；`log_audit` 的灰度审计行按"只增不改"保留。
- **未取得证据 / 缺口（如实标注）**：
  - **FR-22 基本流 ③④「多 Prompt A/B 并行与效果对比看板」未实现**——本轮交付的是"按用户比例放量 + 熔断回滚"这套**发布安全**能力，没有做提示词变体的并行与效果度量对比。
  - **熔断不按功能归因**：异常表只存脱敏 openid 且未带功能标签，故熔断是**全局健康度杀开关**（宁可误关不可漏关），做不到"谁坏了只关谁"。
  - **±2% 抽样仅有单测证据**：运行期用三个真实用户验证了桶号稳定与不洗牌，未以千级真实用户复测。
  - **P95 时延判据**默认不启用（阈值 0），其触发路径只有 `GrayCircuitBreakerTest` 的时延越界用例为证，无运行期样本。
  - **灰度观测台前端页未做**（W2-b）：当前放量走 `PUT /api/configs`、观测走 `GET /api/gray/status`，无 UI，因而本轮**无浏览器实证**。
  - **AUDITOR 运行期样本**依旧缺失（本机只播种 superadmin / operator，连续第三轮同一缺口）；其可读口径仅有 `@PreAuthorize` 与 `RbacSurfaceVerificationTest` 为证。
- **状态**：已提交（见 §9.1）。

### 7.14 迭代 4 W3（FR-23 工具插件化扩展接口）交付与证据

- **范围**：轨道 A 的 FR-23——让"新增一个工具"不再要求改动对话引擎及其派生口径，并把非法 Schema 的检出前移到启动期。设计取舍见 [§2.24](#224-工具插件化扩展接口fr-23--w3已实现)。**无新表**：只写 `sys_config`（V1.0.14 把示例工具加入禁用清单）。
- **落点文件**（12 主源改动 + 1 新工具 + 1 迁移 + 4 新测试类 + 8 处测试构造点）：
  - 契约：`domain/tool/Tool.java`（三件自述元数据 `claimKeywords()` / `monitorDomain()` / `taskIntent()`，全为 `default` 方法 → BR-32 向后兼容）
  - 反查与校验：`domain/tool/ToolRegistry.java`（`claimKeywordsOf` / `claimKeywordUniverse` / `monitorDomainOf` / `taskIntentOf` + 可定位实现类的启动错误）、`domain/tool/JsonSchema.java`（结构校验独立成 `structuralViolations()`；严格重复键解析检出参数名重复；解析失败不再静默）
  - 消费方改造：`application/safety/ActionClaimExtractor`（删静态词表）、`application/admin/MonitorService`（归因域反查）、`application/task/TaskSessionServiceImpl`（意图反查）
  - 既有工具自述：`domain/tool/impl/{ManagePetProfile,QueryExpress,PlanRoute,SynthesizeVoice,RecognizeImage}Tool.java`（逐字迁移原硬编码语义，不漂移）
  - 示例插件：`domain/tool/impl/QueryWeatherTool.java`（新，1 个类、无外部依赖）
  - 那条配置：`resources/db/migration/V1.0.14__dark_launch_weather_tool.sql`（`JSON_ARRAY_APPEND` + `NOT JSON_CONTAINS` 幂等守卫，把 `query_weather` 加入 `orchestration.disabled-tools` = 暗启动，对既有行为零影响）
  - Mock 脚本数据：`resources/mock/llm-scripts.yml`（`weather-toolcall` / `weather-after-tool` 两条规则；匹配词刻意用「查天气」而非「天气」，避免抢走 W6 的「今天天气怎么样」默认回复用例）
  - 测试：`domain/tool/ToolRegistryTest`(11) / `domain/tool/JsonSchemaTest`(8) / `application/safety/ActionClaimExtractorTest`(4，含五件工具语义"迁移不漂移"逐字断言) / `verification/QueryWeatherToolTest`(5) + 夹具 `support/ToolRegistries`（离线构造生产工具集，测试不再另抄一份词表）
- **零改动证明（AC①，以 diff 为据）**：本轮改动集内 `application/orchestrator/AgentOrchestratorImpl.java`、`resources/prompt/**`、`frontend/**` **均无任何改动**；新工具仅凭自身 `@Component` 被 `ToolRegistry` 注入即完成发现、调用、审计与归因。
- **验证（本轮实跑，非推断）**：
  - 全量后端门禁：`mvn -B -o clean verify` → **`Tests run: 485, Failures: 0, Errors: 0, Skipped: 0`**（W2 基线 457 + 本任务 28）+ **`0 Checkstyle violations`** + **SpotBugs `BugInstance size is 0`** + `All coverage checks have been met` + `BUILD SUCCESS`。
  - **干净库冷启动（D8 纪律）**：新建空库 `clawbot_w3` + 真实 `java -jar`（本机 8081 被另一项目的 jar 占用，**未动它**，改 `SERVER_PORT=8092`；带 `WX_TOKEN` / `ADMIN_INIT_PASSWORD`）→ `Successfully applied 15 migrations to schema clawbot_w3, now at version v1.0.14`、**`ToolRegistry 初始化完成，已注册工具数=6 names=[manage_pet_profile, plan_route, query_express, query_weather, recognize_image, synthesize_voice]`**、`Started ClawbotApplication in 10.025 seconds`、`GET /actuator/health = 200`；`GET /api/configs` 可见 `orchestration.disabled-tools = ["query_weather"]`（迁移按预期生效）。
  - **AC②（非法 Schema 阻止启动）**：临时加入一个 Schema 缺根节点 `type` 的 `@Component` 工具 → `clean package` → 真实 `java -jar` → 进程 **退出码 1**，`Caused by: java.lang.IllegalStateException: 工具参数 Schema 非法，启动终止: temp_illegal_schema（com.lumensteward.clawbot.domain.tool.impl.TempIllegalSchemaTool）→ 根节点缺少 type（须为 "object"）`，栈顶直指 `ToolRegistry.validate` → Tomcat 未监听、无 health。取证后**临时类已删除并重新 `clean package`**（`BUILD SUCCESS`），仓库内不留该文件。
  - **暗启动负证**：`disabled-tools=["query_weather"]` 时，同一句「帮我查天气 杭州」的回调在日志中为 `MockLlmClient.chat round=0 finishReason=stop toolCalls=0` → `编排完成 rounds=0 llmCalls=1 tools=0`，回复为 Mock 默认文案，`log_tool_call` 无行——即新工具注册了但对模型不可见。
  - **AC③（放量后可观测）**：`PUT /api/configs` 置 `[]`（reason 必填，BR-25）→ 日志 `配置已更新并即时生效，共 1 项（无需重启）`；同一请求变为 `round=0 finishReason=tool_calls toolCalls=1` → **`QueryWeatherTool - 天气查询（示例插件） city=杭州 hit=true`** → `round=1` → `编排完成 rounds=1 llmCalls=2 tools=1`，对外回复取 round 1 文案；`log_tool_call` 落 1 行 `tool_name=query_weather / status=0 / latency_ms=2 / params_json={"city":"杭州"}`（结果 JSON 带 `data_source=mock-demo`）；`GET /api/monitor/success-rate` → `{toolName:"query_weather",total:1,success:1,successRate:1.0}`；`GET /api/monitor/intent-distribution` → **`weather:1`**（新域按 `INTENT_ORDER` 固定顺序自然追加在既有六项之后）。取证结束已把 `orchestration.disabled-tools` 复原为 `["query_weather"]`。
  - **e2e 回归**：同一实例上 `scripts/e2e-smoke.ps1` **8 PASS / 0 FAIL / 0 SKIP**（含 AC-A2 伪造签名 100/100 拒绝、AC-E1 登录、AC-E3 越权 403、AC-C1 对话建档）——反幻觉词表改由注册表反查后，接入层与档案链路无回归。
- **本机踩到的取证坑（已复现）**：中文 XML 报文若直接拼在 Git Bash 的 `curl` 命令行上，会经 Windows 代码页转码，落库内容为 U+FFFD 替换符（`SELECT HEX(content)` 可见），于是按中文子串匹配的 Mock 规则**永不命中**，看起来像"插件没生效"。可靠做法：报文写成 UTF-8 文件再 `curl --data-binary @file`（`e2e-smoke.ps1` 内部本就显式按 UTF-8 编码字节，故不受影响）。
- **未取得证据 / 缺口（如实标注）**：
  - **SRS FR-23 备选流 2a（工具分组 / 按角色授权）与 4a（工具级限流）未实现**：当前只有全局 `orchestration.disabled-tools` 一刀切，无法按管理员角色或按工具限额。
  - **`IntentType` 仍是封闭枚举**：不占既有意图的插件只能吃 `taskIntent()=null` 的保守默认（不参与话题切换判定）。该默认路径有单测，无运行期样本。
  - **启动校验的其余分支**（参数名重复 / `required` 引用未声明参数 / 根缺 `type` 之外的解析失败 / 工具名冲突 / 名非法）**只有 `ToolRegistryTest`、`JsonSchemaTest` 为证**，本轮只实跑了「缺根 `type`」一条真实启动失败。
  - **AC③ 的看板侧只有 HTTP 接口出参为证**，未做浏览器实证（前端页无改动，`weather` 域会自然出现在同一列表）。
  - **AUDITOR 运行期样本**依旧缺失（本机只播种 superadmin / operator，连续第四轮同一缺口）。
  - **示例工具不接真实数据源**：只回内置三城样例，未收录城市明确返回 `EMPTY_RESULT`（BR-09 不编造）。
- **数据卫生**：取证写在临时库 `clawbot_w3`（1 行 `log_tool_call`、2 次 `CONFIG_UPDATE` 审计、数条 `wx_message`），该库非仓库内容、可随时删除重建；本机 `/tmp` 下的启动日志与请求报文为一次性产物，未入库。
- **状态**：已提交（见 §9.1）。

### 8.1 ADR 列表

- ADR-001 ~ ADR-010：见 SRS（分层单体、SPI 抽象 ADR-010 等）。
- **ADR-011（草案，待 SRS 回写）**：衔光管家为独立项目，不与其他任何项目共享模块 / 共享库 / 共用代码；所需能力各自独立实现。

### 8.2 三道闸门

1. **范围闸门**：3.5 范围冻结 + ADR-001 分层单体。
2. **证据闸门**：诚实性声明 + 证据等级（"完成"须附可复现验证证据）。
3. **质量闸门**：CI 门禁（JaCoCo / Checkstyle / SpotBugs）前移至迭代 1。

### 8.3 文档归档说明

历次文档整合（v1.1：迭代 1 / 2 过程文档；v1.2：迭代 3 Wave 2 证据）将以下过程文档并入本手册后下线（内容已归档于本手册对应章节；原始文件可经 git 历史追溯）：

| 已下线文档 | 归档去向 |
|---|---|
| `MVP产品需求文档.md` | §7.1（迭代 1 范围与目标） |
| `MVP架构设计与任务分解.md` | §3.2（包结构 / 依赖铁律） |
| `QA测试报告.md` | §7.6（独立验证 14 类 / 62 用例） |
| `MVP交付确认与验收三件套.md` | §7.1（验收结论） |
| `class-diagram.mermaid` / `sequence-diagram.mermaid` | §3（架构与依赖） |
| `evidence/index.md` | §7.6（验证方式） |
| `迭代2_增量PRD.md` | §7.2（迭代 2 范围） |
| `迭代2_架构设计与任务分解.md` | §3.2（领域端口上提） |
| `迭代2_S波实现与验收证据.md` | §7.2（S 波清单与验证） |
| `迭代2_class-diagram.mermaid` / `迭代2_sequence-diagram.mermaid` | §3（架构） |
| `本地开发环境说明.md` | §5（部署）、§6.4（e2e 走查） |
| `衔光管家差异化重新设计提案.md` | §1.6（重新定位说明） |
| `迭代3_Wave2实现与验收证据.md` | §7.4（迭代 3 Wave 2 交付与验收证据；v1.2 并入并下线） |

### 8.4 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-19 | 初版工程手册；按重新定位方向撰写；区分已实现 / 规划；确立 ADR-011 |
| v1.1 | 2026-09-19 | 整合版：并入架构详情、迭代交付与证据、本地环境说明、重新定位设计；下线原过程文档（见 8.3） |
| v1.2 | 2026-09-20 | 并入迭代 3 Wave 2 交付证据（§7.4）与七项已实现能力（§2.12~2.18，含补录 A-1 环境自检）；更新数据模型 / 分层 / 前端结构 / 启动校验至 V1.0.9；新增迭代 4 规划（§7.7）；下线 Wave2 独立证据文档（见 8.3） |
| v1.3 | 2026-09-20 | 反映迭代 4 首发任务 W4/B-5 合规报告自动化已实现（§2.22 / §7.8）；新增「接手必读（Handover）」第 9 章（§9）；更新 §1.3 能力清单、§7.7 任务分解与决策记录；README 迭代 4 状态同步 |

---

> 本手册保持衔光既有工程规范（7.6 / 8.2.1 / 8.5 / 9.x / 附录 G）与三道闸门不变，仅 reposition 产品定位与能力权重，并明确衔光为独立项目、不共享任何外部模块。

---

## 第 9 章 接手必读（Handover）

> 本章面向**新接手的同学 / 下一任 agent**，目标是在 5 分钟内建立正确心智模型，并知道怎么跑、怎么验证、下一步做什么。所有结论均指向工作区真实状态（2026-09-21）。

### 9.1 当前仓库状态（git，main 分支）

| 项 | 状态 | 说明 |
|---|---|---|
| 合规模块 6 主源 + 1 测试 | 已提交并推送 main | W4/B-5 后端实现（含 §7.8 提交前修复清单；其运行期缺陷 D9 由 W6 修复） |
| 合规报告前端入口 前端 5 处（后端零改动） | 已提交并推送 main | W4 收尾缺口（§7.12：`/compliance/export` + `compliance.api.ts` + 权限/路由/菜单；浏览器实证含文件落盘） |
| 四层异常埋点 7 主源 + 1 迁移 + 5 测试 | 已提交并推送 main | W1 实现（含 §7.9 验证清单与未取得证据） |
| 个人状态库 14 主源 + 2 迁移 + 6 测试 | 已提交并推送 main | W6 实现（含 §7.10 冷启动与生长 / 强化 / 删除实证） |
| 状态库后台 后端 4 主源 + 2 测试、前端 7 处（含 D10 守卫修复） | 已提交并推送 main | W6-b 实现（含 §7.11 HTTP / 浏览器实证；无新迁移） |
| 灰度开关与熔断回滚 12 主源 + 1 迁移 + 5 测试 + 3 处既有用例扩充 | 已提交并推送 main | W2/FR-22 实现（§2.23 设计 / §7.13 证据：457 绿 + 干净库 AC②③ 与异常流 2a 运行实证；AC① 的 ±2% 抽样为单测实证；无新表） |
| 工具插件化 12 主源改动 + 1 新工具 + 1 迁移 + 4 测试类 + 夹具 + 8 处测试构造点 | 已提交并推送 main | W3/FR-23 实现（§2.24 设计 / §7.14 证据：485 绿 + 干净库 `clawbot_w3` 冷启动、AC② 非法 Schema 真实启动失败、AC③ 日志/`log_tool_call`/看板 `weather` 域；AC① 以 diff 为证；示例工具默认暗启动） |
| `docs/衔光管家项目手册.md` / `README.md` | 已提交并推送 | 与上述同提交，状态口径同步为「已交付」 |
| `e2e-result.txt` / `e2e-8081.txt` | 未跟踪（本地产物，不入库） | 旧 jar 的 e2e 走查记录，**不含 W4 代码**，仅作过程留痕 |
| 本机临时库 `clawbot_w6` / `clawbot_w6b` / `clawbot_w2` / `clawbot_w3` | 未跟踪（本机 MySQL，非仓库内容） | W6 / W6-b / W2 / W3 干净库冷启动所用空库（`clawbot_w3` 现处于 v1.0.14，内含 W3 取证的 1 行 `log_tool_call` 与 2 行配置审计）；可随时删除重建 |

> **接手第一件事**：先 `git status` 与 `git log --oneline -5` 确认迭代 4 进度；本轮验证以 `mvn -B -o clean verify`（**485 绿**）+ §7.13 灰度运行期实证 + §7.14 插件化冷启动/启动失败/看板归因实证为准。

### 9.2 项目一句话地图

- **是什么**：微信原生「个人/家庭执行型管家」（LumenSteward / Clawbot），Spring Boot 3 + Vue 3 分层单体，单拥有方私有部署。
- **已交付（迭代 1~3 + 迭代 4 W1/W2/W3/W4/W6/W6-b）**：微信通道、LLM 对话中枢、6 个 AI 工具、反幻觉/审计/限流/归档/配置热生效/回放/多模型路由/监控看板/任务会话/环境自检/**合规报告导出（W4）**/**四层异常分布补全（W1：L1/L2 落表）**/**个人状态库地基（W6：`biz_memory_item` + 生长 + 溯源 + 召回）**/**状态库后台（W6-b：只读列表 + 纠错删除 + 前端页）**/**灰度开关与熔断回滚（W2：白名单 + 稳定分桶放量 + 30s 自动回滚 + 一键回滚/预览端点，BR-31 回滚路径）**/**工具插件化扩展接口（W3：工具自述 + 注册表反查，新增工具零改引擎；非法 Schema 阻止启动）**。
- **规划未做（迭代 4 其余）**：事件触发引擎、主动执行器与承诺闭环、RAG、成本看板、真 token 流式；灰度的**前端观测台（W2-b）**与 FR-22 的**多 Prompt A/B 并行 + 效果对比看板**、FR-23 的**工具分组授权（备选流 2a）+ 工具级限流（4a）**亦未做（见 §7.13 / §7.14 未取得证据）。
- **权威需求**：`微信Claw助手需求规格说明书.md`（SRS-CLAWBOT-002，只读）。
- **唯一工程文档**：本手册。

### 9.3 本地跑通（依赖 MySQL 8@3306 + Redis@6379）

```bash
# 后端（local + Mock，DB 口令 1234）
cd backend
SPRING_PROFILES_ACTIVE=local DB_HOST=127.0.0.1 DB_PORT=3306 DB_NAME=clawbot \
DB_USERNAME=root DB_PASSWORD=1234 REDIS_HOST=127.0.0.1 REDIS_PORT=6379 \
WX_TOKEN=clawbot-local-token ADMIN_INIT_PASSWORD=Admin@123456 \
java -jar target/clawbot-backend.jar
# 校验：GET /actuator/health = 200；GET /api/doctor（任意已认证角色）

# 前端
cd frontend && npm ci && npm run dev   # http://localhost:5173

# 端到端走查（自带签名生成器）
powershell -File scripts/e2e-smoke.ps1 -WxToken clawbot-local-token -AdminPassword Admin@123456
```

### 9.4 验证清单（怎么确认"真的能跑"）

| 验证 | 命令/动作 | 通过判据 |
|---|---|---|
| 后端单测 | `mvn -B clean verify`（见 §9.6 本机注意项） | 测试全绿（当前基线 **485**），JaCoCo/Checkstyle/SpotBugs 0 违例 |
| 冷启动 | 真实 `java -jar` + `GET /actuator/health` | `Started ...` + 200；Flyway `success=1`（当前至 V1.0.14）；启动日志含 `ToolRegistry 初始化完成，已注册工具数=6`（缺此行即契约校验未过） |
| e2e | `scripts/e2e-smoke.ps1` | 8 PASS / 0 FAIL（本机需 `WX_TOKEN` 与 `-WxToken` 一致，且管理员口令与库内既有 `superadmin` 匹配，否则 AC-E1/C1/E3 会 FAIL/SKIP；**口令未知时的可靠做法：新建空库 + `ADMIN_INIT_PASSWORD` 播种全新 superadmin**，见 §7.10） |
| 合规导出 | SUPER_ADMIN 登录后 `GET /api/compliance/report`；或后台 `/compliance/export` 点「导出合规报告」 | 返回 `text/markdown`，含六大主题与 `biz_memory_item` 基数行，openid 已脱敏；UI 侧文件名取 `Content-Disposition` 且文件真实落盘，OPERATOR 菜单无该项、深链回退 `/dashboard`（见 §7.12） |
| 四层异常埋点（W1） | 伪造签名打一次回调后 `select layer, error_code, count(*) from log_anomaly_event group by 1,2` | 出现 `L1 / SIGNATURE_INVALID` 行；同 MsgId 重投 10 次得 9 条 `MSG_DUPLICATED`；L2 需一条真实 LLM 失败（可 `--llm.base-url` 指向不可达端口） |
| 状态库生长（W6） | `PUT /api/configs` 置 `memory.growth.enabled=true` 后打一次正常回调，再 `select openid,kind,name,extractor,source_trace_id,hit_count from biz_memory_item` | 每个 openid 各自出现带 `AUTO_EXTRACT` + 非空 `source_trace_id` 的条目；再发一条重复消息 → 行数不变、`hit_count` 递增 |
| 状态库后台（W6-b） | SUPER_ADMIN 登录后 `GET /api/memories`、`DELETE /api/memories/{id}`；OPERATOR 打 DELETE | 列表出参 openid 已脱敏、过滤/分页生效；OPERATOR 删 403、SUPER_ADMIN 删 200 且 `log_audit` 落 `MEMORY/DELETE` 行、重复删返回 30005；前端深链硬加载 `/memories` 可渲染（见 §7.11） |
| 灰度与熔断（W2） | `PUT /api/configs` 置 `gray.memory_growth.percent=20` 后反复 `GET /api/gray/preview?feature=memory_growth&openid=…`；再在近窗造出「L2~L4 异常数 / 链路轮次 > 30% 且轮次 ≥ 20」（`log_anomaly_event` + `log_orchestration_trace`）；`POST /api/gray/rollback` 需带 reason | 同一 openid 桶号恒定、比例上调只放量不洗牌；≤60s 内 `percent` 自动变 **0** 且 `log_audit` 出现 `admin_id` **NULL** 的 `GRAY/ROLLBACK` 行（`after_value=percent=0`）；回滚后白名单一并失效（`GRAY_OFF`）、状态库不再新增条目；`percent=101` → 400/10002；OPERATOR 回滚 403；ERROR 只在真实回滚那次，后续越界 tick 降为 WARN（见 §7.13） |

| 工具插件化（W3） | ① 临时加一个 Schema 缺根 `type` 的 `@Component` 工具 → `clean package` → `java -jar`；② 删掉它，把 `orchestration.disabled-tools` 由 `["query_weather"]` 置 `[]`（`PUT /api/configs`，带 reason），用 **UTF-8 文件**发一条内容为「帮我查天气 杭州」的回调；③ `GET /api/monitor/{success-rate,intent-distribution}` | ① 进程退出码 1 且日志给出 `工具参数 Schema 非法，启动终止: {name}（{实现类}）→ 根节点缺少 type`；② 日志出现 `QueryWeatherTool - 天气查询（示例插件） city=杭州 hit=true`、`log_tool_call` 落 `tool_name=query_weather status=0`、对外回复带 `data_source=mock-demo`；③ 成功率表出现 `query_weather`、意图分布出现 `weather` 域（追加在固定顺序末尾）。取证后务必把配置复原为 `["query_weather"]`（见 §7.14） |

### 9.5 立即要做的下一步

1. ~~**提交 W4**~~ **已完成**：合规模块 + 手册口径已提交并推送 main（其运行期缺陷 D9 在 W6 期间修复）。
2. ~~**W1 L1/L2 异常埋点补全**~~ **已完成**：新表 `log_anomaly_event`（V1.0.10）+ 三处埋点 + 看板两表合并，已推送 main（见 §7.9）；其两项运行期缺口已由 §7.10 的干净库冷启动补齐。
3. ~~**W6 个人状态库地基**~~ **已完成**：`biz_memory_item`（V1.0.11）+ 生长管道 + 溯源 + 召回注入 + PII 治理三处接入，已推送 main（见 §7.10）。**生长默认关闭**（每轮多一次模型调用），启用前先确认成本口径。
4. ~~**W6-b 状态库后台**~~ **已完成**：`GET /api/memories` / `DELETE /api/memories/{id}` + 前端只读页 + 浏览器实证，已推送 main（见 §7.11）；顺带修复 D10 深链守卫无限重定向（§7.5）。
5. ~~**补前端入口**（W4 已知缺口）~~ **已完成**：`/compliance/export` + `views/compliance/export.vue` + `services/compliance.api.ts` + 权限项 `compliance:export`（仅 SUPER_ADMIN），后端零改动，浏览器实证含文件真实落盘（见 §7.12）。
6. ~~**W2 灰度开关 + 熔断回滚**~~ **已完成**：`gray.*` 7 键（V1.0.13）+ 稳定分桶分流 + 30s 熔断自动回滚 + `GET /api/gray/{status,preview}` 与 `POST /api/gray/rollback`，FR-22 的 AC②③ 与异常流 2a 有运行实证、AC① ±2% 抽样为单测实证（见 §7.13 / §2.23）。**灰度默认 100 = 不改变既有行为**，放量是"调低比例"而不是"打开开关"；熔断是**全局健康度杀开关**（非按功能归因），一次误异常会关掉所有在放量功能。
7. ~~**W3 工具插件化扩展接口**~~ **已完成**：`Tool` 三件自述元数据 + `ToolRegistry` 反查与启动校验 + 三处消费方去硬编码 + 示例插件 `QueryWeatherTool`（V1.0.14 默认暗启动），FR-23 的 AC②③ 有运行实证、AC① 以 diff 为证（见 §7.14 / §2.24）。**新增工具的正确姿势**：写 1 个 `@Component` 实现 `Tool`（含自述元数据）+ 决定它的灰度/禁用配置；不要再去应用层加工具名清单。**已知边界**：`IntentType` 仍为封闭枚举，工具分组授权与工具级限流未做。
8. **补 AUDITOR 运行期样本**（连续四轮（§7.11 / §7.12 / §7.13 / §7.14）标注的同一缺口）：本地引导时额外播种一个 AUDITOR 账号（`sys_admin_user` 迁移或 `AdminBootstrapRunner` 同机制），即可实证「只读三角色口径」与「不可见导出菜单」，不必再靠权限表声明兜底。
9. **继续迭代 4 规划**（§7.7）：W5 成本看板 → W9 真 token 流式；W2-b 灰度观测台前端页与 FR-22 剩余的「多 Prompt A/B + 效果对比看板」、FR-23 剩余的「工具分组授权 / 工具级限流」一并排期；召回注入的**真实 provider 复核**（§7.10 未取得证据）可并入 W5/W9 一并做。
10. **阻塞项勿动**：W7/W8 主动执行依赖「主动触达通道」确认；G1~G4 须走 SRS 11.3 变更流程生效前，不在测试中引用其编号。

### 9.6 坑位速记（本机实测）

- **Maven 启动器**：本机 `mvn` 脚本在 Git Bash 下因 `MAVEN_HOME` 解析为 unix 路径导致 `找不到 Launcher 类`。可用 Windows 风格路径直接调 java 引导：`java -Dfile.encoding=UTF-8 -cp "D:/apache-maven-3.9.15/lib/boot/plexus-classworlds-2.9.0.jar" -Dclassworlds.conf="D:/apache-maven-3.9.15/bin/m2.conf" -Dmaven.home="D:/apache-maven-3.9.15" -Dmaven.multiModuleProjectDirectory="<backend 绝对路径>" org.codehaus.plexus.classworlds.launcher.Launcher -B -o clean verify`。**离线 `-o` 现已可用**（本地仓库已含 `spring-boot-starter-parent:3.3.4`，实测全量 verify 通过）；`-Dfile.encoding=UTF-8` 必带，否则中文编译错误信息显示为乱码，掩盖真实报错。
- **D8 教训**：单测全绿 ≠ 能启动；多构造器 Bean 须显式 `@Autowired`；结项前必做 `java -jar` 冷启动（W4 为单构造器，已规避）。
- **Flyway**：`CREATE INDEX IF NOT EXISTS` 在 MySQL 报错，改普通 `CREATE INDEX`；改迁移文件名须 `clean package`。
- **`test-compile` 会骗人**：增量状态可能报 `Nothing to compile — all classes are up to date` 而漏掉构造器参数个数变更导致的测试编译失败；**结项信号只认 `clean verify`**。
- **curl 传中文报文（JSON 与 XML 同样中招）**：Git Bash 下把中文直接拼在命令行会被 Windows 代码页转码——`curl -d '{"reason":"中文…"}'` 表现为后端 `10002 参数格式非法`，微信 XML 报文则表现为**落库内容变成 U+FFFD 替换符**（`SELECT HEX(LEFT(content,6))` 见 `EFBFBD` 即可确诊），连带让按中文子串匹配的 Mock 规则**永不命中**、看起来像"插件没生效"（W3 取证实踩，§7.14）。可靠做法：报文写成 UTF-8 文件再 `curl --data-binary @file`，或改用 PowerShell 发体（`e2e-smoke.ps1` 内部显式按 UTF-8 取字节，不受影响）。
- **端口被别的项目占用**：本机 8081 可能被其他项目的 jar 常驻（`netstat -ano | grep :8081` + 查 PID 命令行确认）。**不要停别人的进程**，取证改用 `SERVER_PORT=8092` 之类，并在证据记录里写明实际端口。
- **终端乱码 ≠ 库内乱码**：`mysql` 客户端在 Windows 控制台输出中文为乱码时，用 `SELECT CHAR_LENGTH(col), LENGTH(col)` 判定（UTF-8 汉字应为 1 字符 / 3 字节）再下结论。
- **管理员口令未知**：不要重置或猜测既有库口令；新建空库 + `ADMIN_INIT_PASSWORD` 播种全新 superadmin 是取得 AC-E1/E3/C1 证据的可复现做法（§7.10）。
- **日志/口令**：`tail` 含 token/口令的日志会触发审批拦截，用 `grep -c` 取计数。
- **前端**：勿走 npm shim（managed node 会拉 wsl 被拦），直接 `node.exe node_modules/<pkg>/bin` 调用；`vue-tsc`/`vite`/`eslint`/`vitest` 同理。
- **浏览器验证须用 `http://localhost:5173`**：后端 `clawbot.cors.allowed-origins` 默认只放行 `http://localhost:5173`，用 `http://127.0.0.1:5173` 打开时页面能加载但**所有 `/api` 请求返回 `Invalid CORS request`**（表现为登录后仍停在登录页，易误判为产品缺陷）。
- **出网**：沙箱 `git push` **可能** `errno=10061`（代理中继偶发不通），2026-09-21 实测同一路径推送成功——遇失败先重试一次再下结论，仍不通才转项目外手动推送。推送状态一律以 `git status -sb`（`ahead N`）与 `git log --oneline -1 refs/remotes/origin/main` 为准，不凭记忆写入本文档。
- **Vite 代理指向**：dev server 代理默认 `http://localhost:8080`（`vite.config.ts`），而本机 8080 常被占用、实证 jar 起在 8081——浏览器实证须用 `VITE_API_PROXY_TARGET=http://localhost:8081 npm run dev` 启动，否则 `/api/**` 全部落空。
- **前端 lint 基线**：`npm run lint`（`--max-warnings 0`）在既有 `views/monitor/console.vue` 上有历史格式告警，非门禁脚本的一部分；判断标准是**本次改动文件单独跑 eslint 干净**。
