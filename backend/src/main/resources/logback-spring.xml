<?xml version="1.0" encoding="UTF-8"?>
<!--
  日志配置（FR-21 / 8.2 / G-15）
  - 所有日志统一输出 MDC 中的 traceId（由 TraceIdFilter 写入，G-09）
  - 生产使用结构化 JSON（logstash-logback-encoder），字段与 8.2 约定对齐
  - 禁止在日志中输出未脱敏的 openid / 运单号 / 密钥（8.5）
-->
<configuration>

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="clawbot"/>
    <property name="LOG_PATH" value="${LOG_PATH:-./logs}"/>

    <!-- 开发/测试：可读的文本格式，含 traceId -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{traceId:-}] %logger{40} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 生产：结构化 JSON，字段名对齐 8.2（ts/level/traceId/module/event/costMs/result） -->
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-%d{yyyy-MM-dd}.%i.json.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>openid</includeMdcKeyName>
            <includeMdcKeyName>module</includeMdcKeyName>
            <includeMdcKeyName>event</includeMdcKeyName>
            <customFields>{"app":"${APP_NAME}"}</customFields>
        </encoder>
    </appender>

    <!-- 生产：JSON 文件 + 控制台（容器日志采集） -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="JSON_FILE"/>
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- 非生产：仅控制台 -->
    <springProfile name="!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <logger name="com.lumensteward.clawbot" level="DEBUG"/>
    <!-- 降低框架噪音 -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.apache.ibatis" level="INFO"/>

</configuration>
